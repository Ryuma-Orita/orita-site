# 変更履歴

## 修正履歴

### 2026-08-04 — 初回の `hugo server` で出た3つのエラーを修正

| 問題 | 内容 | 対処 |
|---|---|---|
| `kind in front matter was deprecated` | 論文の front matter で使っていた `kind:` が、Hugo 0.144 で予約語になり削除されていた | 全28ファイルで `kind:` を `pubtype:` に改名。一覧の表示部分も合わせて修正 |
| `failed to render shortcode "icon"` | 授業ページ本文に Academic 独自の `{{< icon ... >}}` が266箇所残っていた | すべて `[動画](URL)` という素のリンクに置換 |
| （同種）`{{% alert %}}` | 注意書き用の Academic 独自記法が28箇所残っていた | 引用ブロック（`> **注意**`）に置換 |
| `languageName was deprecated` | 設定キーの名前が Hugo 0.158 で変更された | `languageName` を `label` に変更 |

あわせて、授業一覧に開講時限（`summary`）を表示するようにし、
テンプレートを Hugo 0.146 以降の新形式（`page.html` / `section.html`）でも
拾えるように複製しました。

### 2026-08-04 (2回目) — レイアウトがテーマに負けていた問題を修正

**症状**：論文一覧に著者名と掲載誌が出ない／トップページの本文（研究興味・学歴）が出ない／
ヘッダーのメニューを押しても遷移しない。

**原因**：私が置いたレイアウトファイルが `layouts/publication/` などの場所にあり、
Hugo がテーマ側の標準レイアウトを優先していました。3つの症状はすべてこれが原因です。

**対処**：

- レイアウトを `layouts/_default/list.html` と `layouts/_default/single.html` に移動。
  ここはテーマとまったく同じパスなので、**プロジェクト側が必ず優先されます。**
  中で「論文」「講演」「授業」を判定して、それぞれの見せ方に分けています。
- トップページはテーマ任せをやめ、`layouts/partials/home/custom.html` で自前で描くように変更
  （`params.toml` の `homepage.layout` を `custom` に）。
- メニューを `pageRef` から確実な `url` 指定に変更。
- プロフィールのリンク（arXiv など）を `profileLinks` として自前で持つように変更。
  テーマのアイコン集に arXiv が無く、リンクが1つ消えていたためです。

**顔写真について**：`assets/img/author.jpg` は現行サイトから引き継いだ既定のシルエット画像です。
実際の写真に差し替えたい場合は、同じファイル名で上書きしてください（正方形推奨）。

### 2026-08-04 (3回目) — 言語設定の書き忘れを修正【根本原因】

**症状**：トップページの本文が出ない／`/publication/` が Page Not Found ／
日本語ページなのにメニューが英語になる／一覧に西暦もラベルも著者も出ない。

**原因**：`languages.en.toml` と `languages.ja.toml` に **`contentDir` の指定が抜けていました。**
そのため Hugo は `content/en` と `content/ja` を「言語別のフォルダ」ではなく、
英語サイト内の `en` `ja` という単なるセクションとして扱っていました。

`hugo server` の出力に出ていた

```
        EN    JA
Pages   221    7
```

がその証拠です。全ファイルが英語サイトに寄っていました。上の症状はすべてこれ1つで説明がつきます。

**対処**：両ファイルの2行目に `contentDir` を追加。

```toml
locale = "ja"
contentDir = "content/ja"   # ← これが抜けていた
```

**確認方法**：`hugo server` の出力の Pages 欄が **EN 60前後 / JA 140前後** になっていれば成功です。
221 / 7 のままなら `config` フォルダが差し替わっていません。

### 2026-08-04 (4回目) — 本文の体裁を整えた

**症状**：ゼミのページで見出しが本文と同じ大きさ、箇条書きに点が付かない、
全体に詰まって読みづらい。トップページの箇条書きも同様。

**原因**：Blowfish は Tailwind CSS ベースで、見出し・リスト・表などの
既定スタイルを**すべてリセット**します。テーマ側はそれを `prose` という
仕組みで復元していますが、私がテンプレートを差し替えたためその復元が効いていませんでした。

**対処**：本文用のスタイルを自前で用意し（`layouts/partials/orita-styles.html`）、
`.Content` を `.orita-prose` で包むようにしました。見出し、箇条書き、表、
引用、リンク、区切り線、コードすべてに体裁が付きます。

色は一切ハードコードしていないので、テーマの配色（`params.toml` の `colorScheme`）を
変えても、ダークモードに切り替えても追従します。

あわせて `content/ja/zemi.md` の `***`（区切り線）を2箇所削除しました。
見出しに罫線が入るようになり、線が二重になっていたためです。

### 2026-08-04 (5回目) — テーマ本来のデザインに全面移行

**ご要望**：講演名の二重表示／「シラバス」の重複／リンクの文言／Blowfish 本来のデザイン。

**対処**：カスタムレイアウトを**すべて削除**し、Blowfish 標準のテンプレートに
描かせる構成へ変更しました。`layouts` フォルダはもうありません。

そのために、front matter にしまい込んでいた情報を本文側へ移しました。

| 種別 | 一覧に出る要約 | 本文 |
|---|---|---|
| 論文 | 著者名 — 掲載誌 | 著者名 / 書誌 / arXiv・Journal リンク / 概要 |
| 講演 | 研究集会名 · 会場 | 研究集会名（リンク付き） / 会場 |
| 授業 | 開講時限 · 場所 | シラバスへのリンク / 参考文献・講義内容 |

情報は何も失われていません。front matter の元データ（`authors` `citation` など）も
そのまま残してあるので、将来また独自の見せ方に戻すこともできます。

**あわせて解消したもの**

- 講演の研究集会名が二重に出ていた（`event` と `summary` が同じ値だった）
- 授業ページの「シラバス」という余分な行
- リンクのボタンが「イベントページ」だった → 「シラバス」に

**テーマ機能を有効化**：パンくずリスト、前後の記事への移動、一覧の要約表示、
ライト/ダーク切り替えボタン、スクロール追従ヘッダー。

**arXiv アイコン**：Blowfish の標準アイコン集に arXiv が無く、リンクが1つ消えていたため、
`assets/icons/arxiv.svg` を追加しました。

**配色**：`colorScheme` を `blowfish`（テーマ既定）にしました。
`params.toml` の1行を書き換えるだけで変更できます。候補は
`ocean` `forest` `avocado` `noir` `one-light` `slate` `congo` `fire` `autumn` `github` など。

### 2026-08-04 (6回目) — 背景アニメーションと本文の左揃え

**ご要望**：見た目が悪い／Blowfish 公式のようなアニメーション背景にしたい。

**対処**：

1. **背景に斜めストライプのアニメーションを追加**
   - 模様の実体：`static/img/bg-pattern.svg`（継ぎ目なく敷き詰まるよう作成）
   - 動きの制御：`assets/css/custom.css`
   - 75秒かけて斜めにゆっくり流れます。ダークモードでは自動的に控えめになります。
   - OS側で「視差効果を減らす」を有効にしている場合は静止します。

2. **本文の左揃え**
   トップページはプロフィールを中央に置くレイアウトなので、その下の本文
   （研究興味・学歴の表・関連リンク）まで中央揃えになり、箇条書きの点だけが
   左端に取り残されていました。`.prose` を左揃えに戻しました。

3. **日本語向けの微調整**
   和文の行間を広げ、和文に存在しない斜体（`*強調*`）を太字で表現するようにしました。

**調整のしかた**

見た目を変えたいときは `assets/css/custom.css` を編集してください。
`themes/` の中は絶対に触らないでください（テーマを更新すると消えます）。

- 背景を弱く／強くしたい → `opacity: 0.9` の数値を変える（0で消える）
- 流れる速さを変えたい → `75s` の数値を変える（大きいほど遅い）
- 背景を消したい → `body::before { ... }` のブロックごと削除

**トップページを公式サイトのような全面背景にしたい場合**

`config/_default/params.toml` の `[homepage]` を次のように変えると、
写真を全面に敷いたレイアウトになります（別途、背景写真が必要です）。

```toml
[homepage]
  layout = "background"
  homepageImage = "img/background.jpg"   # assets/img/ に置く
  layoutBackgroundBlur = true            # スクロールでぼける
```

### 2026-08-04 (7回目) — 公式の background レイアウトに切り替え

公式サイトと同じ全面背景のレイアウトにしました。

```toml
# config/_default/params.toml
[homepage]
  layout = "background"
  homepageImage = "img/background.jpg"
  layoutBackgroundBlur = true
```

配色も濃紺系の `slate` に変更しました。

**背景画像について**

`assets/img/background.jpg`（2560×1440, 69KB）は公式サイトの背景を参考に
新しく作ったものです。テーマ付属の `exampleSite/assets/img/` にある写真
（ocean.jpg など）は**テーマ作者個人のデモ用写真**で、MITライセンスが
カバーしているのはコードであって写真ではありません。念のため使わずに済むよう
自前で用意しました。

差し替えたい場合は、好きな画像を `assets/img/` に置いて
`homepageImage` のファイル名を変えるだけです。横長（16:9 程度）を推奨します。

**内側のページ**

`assets/css/custom.css` の背景模様は残してあります（濃度を 0.9 → 0.55 に下げました）。
トップの濃紺と内側の淡い模様でつながりが出ます。不要なら
`body::before { ... }` のブロックごと削除してください。

**顔写真**

トップページが濃紺になったため、現在のシルエット画像（黒地に青の人型）は
背景に沈んで見えにくくなります。実際の写真に差し替えることをおすすめします。
`assets/img/author.jpg` を同じファイル名で上書きしてください（正方形推奨）。

### 2026-08-04 (8回目) — 背景の動きを一本化

**症状**：トップページで、本文の外側（下部）だけが動き、本文の背後は静止画だった。

**原因**：背景が二重になっていました。

- トップページの背景 → `homepageImage`（静止画 `background.jpg`）
- ページ全体の背景 → `custom.css` の `body::before`（動くストライプ）

前者が後者を覆い隠すため、はみ出した下部だけが動いて見えていました。

**対処**：CSS側の背景レイヤーを削除し、**背景画像そのものに動きを持たせました。**

`assets/img/background.svg` は SVG の中に CSS アニメーションを埋め込んであります。
90秒かけて斜めに流れ、開始と終了のコマが完全に一致するので継ぎ目なくループします
（生成時に画素単位で照合済み、差分ゼロ）。

`params.toml` に `disableImageOptimization = true` を追加しました。
SVG は Hugo の画像処理を通せないため、そのまま使うための設定です。

**静止画に戻したい場合**

`assets/img/background.jpg` も同梱してあります。`params.toml` の

```toml
homepageImage = "img/background.svg"
```

を `.jpg` に変えるだけで静止画になります。

**動きの速さを変えたい場合**

`assets/img/background.svg` をテキストエディタで開き、冒頭付近の

```
.bars { animation: orita-drift 90s linear infinite; }
```

の `90s` を変えてください。数値が大きいほどゆっくりです。

### 2026-08-04 (9回目) — 流れる向きの変更と、内側ページの背景の復活

**ご要望**：トップページ以外の背景が消えた／右上から左下へ丸バーが進む動きにしたい。

**対処**：

1. **向きを右上→左下に変更**
   丸バーは左下から右上に伸びた形なので、バーが自分の向きに沿って
   右上から左下へ流れていく動きになります。
   `translate(-720px, 720px)` に変更しました。

2. **内側ページの背景を復活**
   前回、二重表示を解消するために CSS 側の背景を削除しましたが、
   トップページ以外が真っ白になっていました。
   同じ意匠・同じ向き・同じ速さ（90秒）で戻しました。
   ページを移っても流れが途切れて見えません。

   - トップページの背景：`assets/img/background.svg`（濃紺・動き内蔵）
   - それ以外のページ：`static/img/bg-pattern.svg` + `assets/css/custom.css`（淡色）

**検証したこと**

- トップ背景：アニメーションの開始コマと終了コマを画素単位で照合し、差分ゼロを確認
- 内側ページのタイル：2×2 に並べて継ぎ目が出ないことを目視確認

**速さと濃さの調整**

| 変えたいもの | 場所 |
|---|---|
| トップの速さ | `assets/img/background.svg` の `90s` |
| 内側ページの速さ | `assets/css/custom.css` の `90s` |
| 内側ページの濃さ | `assets/css/custom.css` の `opacity: 0.5` |
| 逆向きにしたい | 両方の `translate` の符号を入れ替える |

**両方を同じ数値にしておくと、ページを移ったときに動きが揃います。**

### 2026-08-04 (10回目) — 授業ページの「動画」をアイコンに

授業ページの講義内容に並ぶ `[動画](URL)` を、YouTube のアイコンに変えました（266箇所）。

Blowfish のアイコン機能 `{{< icon "youtube" >}}` を使っています。
アイコンの実体は `assets/icons/youtube.svg` に自前で用意しました
（テーマ側の標準アイコン集に入っていなくても確実に表示されます）。

色はリンクの文字色に自動で合わせるので、ライト/ダークどちらでも見えます。
マウスを乗せると「動画」というツールチップが出ます。

**大きさや色を変えたい場合**

`assets/css/custom.css` の末尾、`.prose a svg` のところで調整できます。

- 大きさ → `width` と `height` の `1.25em`
- YouTube の赤にしたい → `assets/icons/youtube.svg` の `fill="currentColor"` を
  `fill="#FF0000"` に変更

### 2026-08-04 (11回目) — 体裁の総点検

スクリーンショット10枚を確認して見つかった問題と対処です。

**1. 数式が生の LaTeX のまま表示されていた（最重要）**

`$\mathbb{Z}\oplus\mathbb{Z}$` がそのまま文字として出ていました。
Blowfish で数式を表示するには、そのページに `{{< katex >}}` を書く必要があります。
数式を含む29ファイル（論文9本・授業20本）の本文冒頭に自動で追加しました。

今後、数式を含むページを新しく作るときは、本文の一番上に

```
{{< katex >}}
```

の1行を入れてください。この行自体は画面に何も表示しません。

**2. ゼミのページの日付が「0001年1月1日」になっていた**

日付を持たないページなのに日付欄が出ていました。
`content/ja/zemi.md` の front matter に `showDate: false` を追加して非表示にしました。
あわせてパンくずと前後移動も切っています（一覧に属さない独立ページのため）。

**3. 授業の個別ページが空に近かった**

シラバスへのリンクしかないページが多かったので、開講情報
（「M向け，第2学期木曜3限 · 新潟大学 大学院総合学術研究科」など）を
本文にも出すようにしました（85ファイル）。

**4. 顔写真が濃紺の背景から浮いていた**

黒地に青い人型のシルエットが、濃紺の背景の上で黒い塊に見えていました。
サイトの配色（紺＋淡いスレート）に合わせて塗り直しました。
元の画像は `assets/img/author-original.jpg` に残してあります。

**これはあくまで仮の画像です。** 実際のお写真に差し替えるときは
`assets/img/author.jpg` を同じファイル名で上書きしてください（正方形推奨）。

---

## ライトモードについて

現在の設定では、閲覧者のOSの設定に合わせてライト/ダークが自動で切り替わります。
デザインを濃紺の背景に合わせて組んでいるため、**ライトモードでは
トップページの背景が淡いグレーになり、印象がかなり変わります。**

常にダークで見せたい場合は `config/_default/params.toml` を次のように変えてください。

```toml
defaultAppearance = "dark"
autoSwitchAppearance = false
```

### 2026-08-04 (12回目) — フォルダ全体の精査と最適化

1. **空の `post` セクションを削除**（en/ja とも中身は見出しページのみ）。
   旧URL `/post/` `/ja/post/` はトップへ転送するよう `_redirects` に追加。
2. **使われていなかった `tag:` を61ファイルから除去。**
   Hugo のタクソノミーは `tags:`（複数形・リスト）で書く必要があり、
   単数形の `tag:` はどのページも生成しない死んだデータでした。
   タクソノミー機能自体も明示的に無効化（`taxonomies.toml`）。
3. **sitemap 設定を `hugo.toml` に一本化**し、未使用の taxonomy 表示設定を削除。
4. **論文ページ冒頭を整理。** 著者・書誌・リンクが3つの孤立した段落に
   分かれて間延びしていたのを、1ブロックにまとめました（28ファイル）。
5. **配布物から除外**: `author-original.jpg`（旧アバター）と
   `background.jpg`（静止画版の背景）。サイトの表示には使われていません。
6. **README を手順書に戻し、変更履歴をこの CHANGELOG.md に分離。**
   README 内の古い記述（存在しない layouts フォルダ、旧 `kind:` 書式、
   post の記述）も現状に合わせて更新しました。

### 2026-08-04 (13回目) — 数式の表示とアイコンの調整

**1. 数式が表示されず、バックスラッシュが二重になっていた（最重要）**

授業ページに `$\\mathbb{R}^n$` と表示されていました。原因は2つ重なっていました。

- 旧サイトでは Markdown の処理を通すために `\\` と2本書く必要があったが、
  今の設定（`markup.toml` の passthrough）では数式の中身をそのまま渡すため、
  1本で書かなければならない。35箇所を修正しました。
- KaTeX がインラインの `$...$` を既定で拾っていなかった。
  `layouts/partials/extend-footer.html` を追加し、読み込み後に
  `$...$` を含む区切り記号で描き直すようにしました。
  KaTeX を使わないページでは何も起きません。

**今後、数式を書くときはバックスラッシュ1本です。**
`$\mathbb{R}^n$` と書いてください（`$\\mathbb{R}^n$` ではありません）。

**2. YouTube アイコンが上にずれていた**

大きさを 1.25em → 1em にし、ベースラインに合わせました。
CSSはテーマの class 名ではなく `viewBox` の値で狙い撃ちしているので、
テーマを更新しても壊れません。

**3. arXiv のアイコン**

汎用の書類アイコンでは Google Scholar・ResearchGate・ORCID と並んだときに
浮いていたため、「arXiv」というワードマーク（文字そのもの）に変えました。
横長なので、正方形に潰されないよう CSS を添えてあります。
文字色に自動で合わせるので、ライト/ダークどちらでも見えます。

### 2026-08-04 (14回目) — arXiv 公式ロゴマークの採用

いただいた公式ロゴマーク（`arxiv-logomark-small.svg`）を採用しました。
使用箇所は3か所です。

| 場所 | 見え方 |
|---|---|
| トップページのプロフィール | 他のリンクアイコンと並ぶ |
| 各論文ページ | 本文のリンク行が `[ロゴ] · Journal Page` に |
| 論文一覧ページ | 各項目のうしろにロゴが付く |

**技術的な補足**

- ロゴは赤（#b31b1b）とグレー（#bdb9b4）の固定色なので、
  ライト/ダークどちらでもそのまま見えます。
- 同じページに何度も埋め込むため、SVG 内の重複する `id` を除去し、
  代わりに `aria-label="arXiv"` を付けました。
- 縦長（17.7 × 24.3）なので、正方形に潰されないよう CSS を添えています。
- 一覧ページへの追加は Blowfish の拡張ポイント
  `layouts/partials/extend-article-link.html` を使っています。
  **テーマ本体は一切編集していません。**

**注意**：一覧ページへの追加は、テーマ側の拡張ポイントの仕様に依存しています。
一覧にロゴが出ない場合は教えてください。別の方法に切り替えます。

### 2026-08-04 (15回目) — DOI アイコンと、トップページの動画リンク

**1. Journal Page → DOI アイコン**

いただいた公式ロゴを `assets/icons/doi.svg` に組み込みました。
元ファイルには `viewBox` が無く固定サイズ（130×130）だったため、
拡大縮小できるよう `viewBox="0 0 130 130"` を付けて固定サイズを外しています。

**ただし、リンク先が実際に DOI のものだけをアイコンにしました（18箇所）。**
残り3箇所は DOI ではないので、文字のまま残しています。

| ファイル | リンク先 | 扱い |
|---|---|---|
| 06-Application-of-fragmentation-norms（日英） | 京大数理研 講究録 | 「Journal Page」の文字のまま |
| 09-Linear-math | 培風館（書籍の販売ページ） | 「出版社のページ」の文字のまま |

DOI ではないリンクに DOI のロゴを付けると誤解を招くため、この扱いにしました。
文字のままで構わなければこのままに、統一したい場合は別のアイコンを用意します。

**2. トップページの動画リンクを YouTube アイコンに**

```
にいがた"知の革新"STELLAプログラムの動画（トポロジー） [▶]
```

文章は普通の文字になり、末尾の YouTube アイコンだけがリンクになります。

3つ目の「新潟大学ウェブマガジンの記事」は動画ではないため、
文字リンクのまま残してあります。

**3. アイコンのCSSを汎用化**

アイコンが3種類（YouTube・arXiv・DOI）に増えたので、
個別指定をやめて「本文中のアイコンは文字と同じ高さに揃える」という
共通ルールにまとめました。縦長の arXiv だけ例外指定しています。

### 2026-08-04 (16回目) — 背景の一本化・記事アイコン・速度

**1. 背景が二重になり、上部だけ別の背景になっていた**

トップページには2種類の背景が重なっていました。

- 上部（縦に短い部分）… `homepage.layout = "background"` が敷く背景画像
- その下……………… `custom.css` が敷く、全ページ共通の動く背景

境目がはっきり見えて不格好だったため、**トップページ専用の背景画像をやめ、
全ページ共通の動く背景1枚に統一しました。**
`homepage.layout` を `background` から `profile` に戻しています。

背景画像を使う設定は `params.toml` にコメントとして残してあるので、
戻したくなったら3行のコメントを外し、`custom.css` の `body::before` を消してください。
`assets/img/background.svg` も残してあります。

**2. 「新潟大学ウェブマガジンの記事」を記事アイコンに**

`assets/icons/article.svg` を新しく作りました（新聞・記事を表す図形）。
YouTube アイコンと同じ塗りの太さなので、3つ並んでも揃って見えます。

**3. 背景の流れる速さ**

90秒 → **45秒**（約2倍の速さ）にしました。

変えたいときは `assets/css/custom.css` の

```
animation: orita-bg-drift 45s linear infinite;
```

の数値です。**小さいほど速く**なります。20s くらいまでは自然に見えますが、
それより速いと文字が読みにくくなるかもしれません。

### 2026-08-04 (17回目) — ファビコン・共有画像・見出しフォント・アクセントカラー

**1. ファビコンと共有画像（幾何学タイル模様）**

参考画像の雰囲気で、サイトの紺系パレット + 臙脂1粒の幾何学タイル模様を
自前生成しました。既製素材は使っていないので権利関係はクリーンです。

- 共有画像 `assets/img/ogp.png`（1200×630）:
  タイル模様の上に「折田 龍馬 / Ryuma ORITA / 新潟大学理学部 准教授」。
  X・Slack・LINE などでリンクを共有したときに表示されます。
- ファビコン一式（`static/` 直下）:
  favicon.ico / 16 / 32 / apple-touch-icon / android-chrome 192・512 / site.webmanifest。
  小さいサイズで潰れないよう、2×2 の大胆な構成（四分円・円・三角、臙脂1枚）に
  設計してあります。16px でも判別できることを確認済み。

**2. 見出しを明朝体（Zen Old Mincho）に**

Google Fonts から読み込み、main 内の h1〜h3 だけに適用しています。
本文・メニューはゴシックのままなので読みやすさは変わりません。
名前・「研究興味」「論文等」などの見出しに品が出ます。

**3. アクセントカラー: 臙脂 (#b31b1b)**

arXiv のロゴと同じ赤に揃えました。使いどころは控えめに3か所だけ:

- h2 見出しの下の短い罫（2.2rem幅）
- リンクにマウスを乗せたときの色（アイコンリンクは除く）
- 文字を選択したときの背景色

ダークモードでは自動的に少し明るい赤 (#e06060) になります。

**調整**: 色を変えたいときは `custom.css` の `--orita-accent` の2行。
フォントを変えたいときは先頭の `@import` の family= と、
`font-family` の指定を書き換えてください。

### 2026-08-04 (18回目) — アクセントカラーを緑系に変更

臙脂 → 緑系に変更しました。

- **ライトモード**: ビリジアン `#2f7d63`（深い青緑。白地で読める濃さ）
- **ダークモード**: ミルキーグリーン `#a9d5bd`（濃紺の上で映える淡さ）

共有画像（ogp.png）とファビコン一式も、臙脂のタイルを
ビリジアンに差し替えて再生成しました。CSS・画像とも同系統で揃っています。

微調整したいときは `assets/css/custom.css` の `--orita-accent` の2行だけです。

### 2026-08-04 (19回目) — サイト全体を緑系の配色に統一

**1. テーマの配色**: `colorScheme = "forest"`（Blowfish 組み込みの緑系）に変更。
リンク色・ボタン・見出しなど、テーマが管理する色が全て緑基調になります。

**2. 背景模様**: 内側ページの流れる斜めバーを、深いモスグリーンの下地
（#0f1f1a → #1b3a2f）+ セージグリーンのバー (#5f9c82) に再生成。
予備として残してあるトップ用 background.svg も同じ配色にしました。

**3. 共有画像とファビコン**: タイルのパレットを緑系
（濃緑・ビリジアン・セージ・ミルキーグリーン）で再生成。
**差し色として金茶 (#e9c46a) のタイルを一粒だけ**入れています。
緑一色だと平板になるためで、深緑×金は品よくまとまる定番の組み合わせです。
不要なら言ってください。緑だけの版を作ります。

**4. その他**: site.webmanifest のテーマ色も緑に更新。
CSS のアクセント（ライト=ビリジアン / ダーク=ミルキーグリーン）は前回のまま。

### 2026-08-04 (20回目) — アバターと背景濃度の調整

**1. アバターを緑系に塗り直し**

サイト全体を緑にしたあと、中央のシルエット画像だけが紺のまま取り残されて
浮いていました。深緑の下地 + ミルキーグリーンの人型に塗り直しました。

元の画像（黒地に青）は `assets/img/author-original.jpg` に残してあります。
**これは仮の画像です。** 実物のお写真に差し替えるときは
`assets/img/author.jpg` を同名で上書きしてください（正方形推奨）。
写真にすれば配色から浮く問題自体がなくなります。

**2. 背景模様の濃さをモード別に調整**

ライトモードで斜めバーが本文と競っていたため、明暗で濃さを変えました。

| | 変更前 | 変更後 |
|---|---|---|
| ライト | 0.5 | **0.24**（控えめに） |
| ダーク | 0.22 | **0.30**（もう少し出す） |

`assets/css/custom.css` の `opacity` の2行です。
本文の読みやすさを優先するならライト側をさらに下げてください。

**3. arXiv アイコンの赤**

緑の中で1つだけ赤が残りますが、公式ロゴの色なので変更していません。
色を変えると別物になってしまうためです。

### 2026-08-04 (21回目) — 顔写真を実物に差し替え

いただいた写真（カランク/南仏と思われる海辺）を加工して設置しました。

**加工内容**

- 上半身（頭上に少し余白 〜 腰のあたり）で正方形に切り出し
- 背後の人物2名を強くぼかして判別できない状態に
  （プライバシーの観点でも、背景として沈める観点でも）
- 背景の彩度を落として、被写体だけが浮き上がるように
- 逆光で眠い写真だったため、被写体側のみコントラスト +13%、彩度 +10%、
  軽いシャープネスをかけて締めています

被写体と背景の境目は楕円マスクを大きくぼかしてあるので、
切り抜いた不自然さは出ません。

**ファイル**

- `assets/img/author.jpg` … 新しい写真（1000×1000）
- `assets/img/author-silhouette.jpg` … 直前まで使っていた緑のシルエット（予備）
- 元の黒地シルエットは削除しました

`languages.*.toml` に `imageQuality = 96` を追加し、縮小時のにじみを抑えています。

**切り出し位置を変えたい場合**は、元写真とご希望（もっと寄る／引く／顔だけ 等）を
お知らせください。作り直します。

### 2026-08-04 (22回目) — 写真の寄りとトップページの構成

**1. 顔写真をさらに寄せた**

肩・胸より上の構図にしました（頭〜胸上部）。
160px の円で表示したときに顔がはっきり見えることを確認済みです。
背景のぼかしと色調整は前回と同じ処理です。

**2. トップページの構成変更**

見出しの並びを次のようにしました。

```
自己紹介 → 研究興味 → 関連リンク → 学位 → 経歴
```

- 「学歴」→「学位」に改称（英語ページは Education → Degrees）
- 「関連リンク」を「研究興味」の直後へ移動
- 「経歴」を新設（学位の直後）

**3. 経歴の表**

いただいた6件を、期間・職位・所属の3列の表にしました。
所属はリンク付き、所在地は括弧書きで所属欄に入れています
（4列にすると狭い画面で読みにくくなるため）。

所在地を独立した列にしたい場合や、市名の表記を変えたい場合は言ってください。

英語ページにも Experience として同じ内容を英語表記で追加しました。
機関名の英訳が実際の表記と違う場合は教えてください。

**4. 表の折り返し**

経歴の表は文字が長いため、狭い画面では横スクロールできるようにしました。
「期間」の列だけは途中で改行されないようにしています。

### 2026-08-04 (23回目) — 写真のぼかし除去・所在地削除・速度3倍ほか

**1. 顔写真のぼかしを除去**

背景をぼかす処理をやめ、写真そのままの状態にしました。
逆光対策の色調整（コントラスト +13%、彩度 +10%、軽いシャープ）だけ残しています。
背後の岩肌と海がくっきり出て、風景として成立するようになりました。

**2. 経歴の表から所在地を削除**（日英とも）

**3. 背景の丸バーを3倍速に**

45秒 → **15秒**。`assets/css/custom.css` と `assets/img/background.svg` の
両方を同じ値にしてあります。

**4. 表の見出し行（「年 学位 大学」など）を非表示に**

Markdown の表は見出し行が必須なので、CSS で隠しています。

```css
.prose table thead { display: none; }
```

表があるのはトップページの2ファイルだけなので、他ページへの影響はありません。
将来どこかの記事に表を書いた場合も見出しが消えるので、
出したくなったらこの1行を消してください。

**5. 英語版の経歴をいただいたデータに差し替え**

前回は私が訳した機関名でしたが、正式な英語表記とリンク先に置き換えました。

### 2026-08-04 (24回目) — リンク色・表の改行・背景の密度

**1. リンクの色が鮮やかすぎた**

原因は、私が書いたリンク色の指定がテーマ側に負けていたことです。
`:has()` を使った複雑なセレクタが効かず、テーマ既定の明るい青緑が
そのまま出ていました。単純なセレクタ + `!important` に書き換え、
確実に効くようにしました。

色も落ち着かせています。

| | 変更前 | 変更後 |
|---|---|---|
| ライト | テーマ既定の明るい青緑 | `#2a5445`（文字色に近い深緑） |
| ダーク | テーマ既定の明るい青緑 | `#c3d8cd`（明るすぎない淡緑） |

リンクであることは**下線**で示し、色はほとんど主張させない方針です。
マウスを乗せたときだけビリジアンになります。

**2. 経歴の変な位置での改行**

和文が語の途中で折れていました（「マス・フォア・インダストリ / 研究所」など）。
表のセルに `word-break: keep-all` を指定し、和文は空白や句読点でのみ
折り返すようにしました。

「日本学術振興会 特別研究員-PD」がハイフンの位置で折れる問題は、
ハイフンを非改行ハイフン（U+2011）に置き換えて解決しています。
見た目は普通のハイフンと同じです。

**3. 丸バーの密度を2倍**

タイルあたり5本 → **10本**。速度は前回の15秒のままです。
密度を変えたいときは言ってください（生成し直しになります）。

### 2026-08-04 (25回目) — ダークを既定に・プロフィール写真の拡大を無効化

**1. ダークテーマを既定に**

```toml
defaultAppearance = "dark"
autoSwitchAppearance = false
```

`autoSwitchAppearance` を `false` にしたので、閲覧者のOS設定に関係なく
常にダークで表示されます。**右上のボタンでの手動切り替えは従来どおり可能**で、
選んだ状態はブラウザに記憶されます。

OS設定に追従させたい場合は `autoSwitchAppearance = true` に戻してください。

**2. プロフィール写真をクリックしても拡大しないように**

```toml
disableImageZoom = true
```

これはサイト全体の設定です。将来どこかの記事に画像を貼ったとき、
その画像も拡大しなくなります。記事の図は拡大できたほうが便利なので、
そのときはこの行を消し、代わりに `custom.css` のクリック無効化だけを
残す形にしてください。

念のため `custom.css` にも保険を入れてあります。

```css
img[src*="author"] { pointer-events: none; }
```

テーマ側の実装が変わっても、プロフィール写真だけは反応しません。

### 2026-08-04 (26回目) — リンク色の適用範囲を全体へ

**症状**：ヘッダーのメニューや論文一覧のタイトルにマウスを乗せると、
テーマ既定の明るい青緑になる。

**原因**：私が書いたリンク色の指定を `.prose a`（＝本文の中）に
限定していたため、その外側には効いていませんでした。
ヘッダーのメニュー、一覧のタイトル、フッターはいずれも `.prose` の外です。

**対処**：`main` / `header` / `footer` の中のリンク全般に、
マウスオーバー時のアクセント色（ライト=ビリジアン、ダーク=ミルキーグリーン）が
効くようにしました。

平常時の扱いは場所によって分けています。

| 場所 | 平常時 | マウスオーバー |
|---|---|---|
| 本文中のリンク | 落ち着いた深緑 + 下線 | アクセント色 |
| メニュー・一覧のタイトル | 文字色のまま（下線なし） | アクセント色 |

**あわせて修正**：論文一覧で、タイトルに下線が付き、その下に arXiv のロゴが
不自然に落ちていました。ロゴをブロック要素として 独立させ、
タイトルの行を乱さないようにしました。

### 2026-08-04 (27回目) — 丸バーの速度をさらに2倍

15秒 → **7.5秒**（1周期あたり）。

`assets/css/custom.css` と `assets/img/background.svg` の両方を同じ値にしています。
速さを変えるときは、この2ファイルの `7.5s` を揃えて書き換えてください。
**数値が小さいほど速く**なります。

### 2026-08-04 (28回目) — 名前と肩書きの間隔

**症状**：トップページで名前（折田 龍馬 / Ryuma ORITA）と
肩書き（新潟大学理学部 准教授）が接近しすぎている。

**原因**：見出しを明朝体にしたとき、`font-weight` だけ指定して
行の高さと余白はテーマ既定のままにしていました。
明朝体はゴシック体より字面が大きいため、同じ余白では詰まって見えます。

**対処**：

- 名前の行の高さを 1.3 に、下の余白を 0.55rem に
- 肩書きの上の余白を 0.55rem に、行の高さを 1.6 に
- 肩書きとリンクアイコンの間を 0.9rem に

数値は `assets/css/custom.css` の `main h1` 付近です。
まだ詰まって見える／空きすぎる場合は `margin-bottom` と
`margin-top` の値を増減してください。

### 2026-08-04 (29回目) — 最終点検と最適化

機械的な点検で見つかった3点を修正しました。

**1. 画像最適化を有効に戻した**

`disableImageOptimization = true` は、トップページの背景に SVG を使うために
入れた設定でしたが、その方式をやめたので不要になっていました。
`false` に戻し、Hugo が表示サイズに合わせて画像を縮小・WebP化するようにしています。

※ 将来 `homepage.layout = "background"` に戻す場合は `true` に戻してください。

**2. 顔写真のサイズを適正化**

1000×1024px（261KB）→ **720×720px（139KB）**。
表示は最大でも 320px 程度なので、720px で 2倍解像度として十分です。
上の最適化と合わせて、実際に配信されるのはさらに小さくなります。

**3. 未使用ファイルの削除**

- `assets/img/author-silhouette.jpg`（緑シルエット、参照ゼロ）
- `layouts/partials/extend-article-link/simple.html`（重複。本体だけで動作確認済み）
- `themes/ここにblowfishを入れます.txt`（設置完了済みのため）

**点検して問題がなかったもの**

- TOML 全8ファイル、front matter 全201ファイル、CSS の括弧対応
- 日英の論文14本・講演40件がすべて対応（欠けなし）
- 全ページに `summary` と本文あり
- `themes/` は未編集（テーマを安全に更新できる状態）

### 2026-08-04 (30回目) — 連絡先（メール）の追加

プロフィールのリンク欄の先頭にメールアイコンを追加しました。

**迷惑メール対策**

アドレスは**サイトのどこにも文字列として書かれていません。**

1. 設定ファイルにはダミーのURL（`https://example.invalid/mail`）だけを書く
2. `layouts/partials/extend-footer.html` の JavaScript が、閲覧時に
   断片（`orita` / `math` / `sc` / `niigata-u` / `ac` / `jp`）を組み立てて
   `mailto:` に差し替える
3. `@` も文字として書かず `String.fromCharCode(64)` で生成

収集ボットはページのHTMLを読むだけなので、この形なら拾えません。
HTML、RSS、サイトマップのいずれにもアドレスは現れません。

**アドレスを変更するとき**

`layouts/partials/extend-footer.html` の

```js
var u = "orita", d = ["math", "sc", "niigata-u", "ac", "jp"].join(".");
```

の部分を書き換えてください。

**制約**

JavaScript を切っている閲覧者にはリンクが機能しません（ダミーURLのまま）。
ただし現在ほぼ全ての閲覧者で有効なので、実害はほぼありません。
確実性を優先するなら、アドレスを平文で書く方法もありますが、
迷惑メールとの引き換えになります。

### 2026-08-04 (31回目) — ソースの精読と整理

`custom.css` を全文精読し、動作を変えずに整理しました。
新旧の実効的な指定を機械照合し、差分が「重複の統合」だけであることを
確認したうえで差し替えています。

**直したもの**

1. **古いコメント3箇所**
   - 「臙脂の短い罫」→ 実際はアクセント変数（緑）なので表現を修正
   - 背景の説明が「トップページの背景と同じ意匠」のままだった
     （現在は background.svg は未使用の予備）
   - 表の見出し行非表示の注記が「ほかのページに影響しない」と
     書いてあったが、正しくは「.prose 内の全ての表に効く」

2. **重複していた指定2組を統合**
   - `.prose table td` の折り返し関連が2ブロックに分かれていた
   - `.prose a:has(svg)` がセクション4と5の両方にあった

3. **冒頭に目次を追加**し、各セクションの調整箇所（速さ・濃さなど）を
   コメントに明記しました。今後の手直しが楽になります。

**精読して問題がなかったもの**

- layouts/ の3ファイル（KaTeX・メール・一覧のarXiv）
- config/ の8ファイル（前回の点検から変更なし）
- netlify.toml（Hugo 0.164.0 指定 = 手元と同じ）

### 2026-08-04 (32回目) — メールリンクの差し替えが効かない問題

**症状**：メールアイコンを押すと、宛先が `https://example.invalid/mail` になる。

**原因**：JavaScript による差し替えが動いていませんでした。
Blowfish は `email` キーの値の先頭に `mailto:` を自動で付けるため、
差し替え前の href は `mailto:https://example.invalid/mail` という形になります。
そのままメールソフトに渡ると、ダミーURLが宛先として解釈されます。

**対処**：スクリプトを三段構えにしました。

- 読み込み直後、`DOMContentLoaded`、`load` の3回実行
- 対象を3パターンに拡張
  （`data-mail` 属性 / ダミーURLを含む href / `mailto:http` で始まる href）
- 差し替え後はどのパターンにも一致しなくなるので、二重処理は起きない

**確認方法**

ページ上で右クリック →「ページのソースを表示」→ Ctrl+F で `niigata-u` を検索。
`var d = ["math", "sc", "niigata-u", ...]` という行が見つかれば、
`layouts` フォルダが正しく設置されています。
見つからない場合は `layouts` の差し替え漏れです。

### 2026-08-04 (33回目) — メニューのマウスオーバー色

**症状**：ヘッダーのメニューにマウスを乗せると、
テーマ既定の明るい青緑のままになる。

**原因**：メニューの文字は `<a>` の中の `<span>` に入っており、
その `<span>` 自体にテーマの色指定があります。
`<a>` に色を指定しても、子の `<span>` が自前の色を持っているため
上書きされませんでした。

前回「サイト全体に効かせた」と書きましたが、届いていたのは
`<a>` が直接文字を持つ場合だけでした。

**対処**：`<a>` の中の子要素にもホバー色が届くようにしました。
ただし SVG アイコン（arXiv・DOI の公式ロゴ）は色を保つため除外しています。

```css
header a:hover *:not(svg):not(svg *) { color: var(--orita-accent) !important; }
```

### 2026-08-04 (34回目) — メニューのホバー色（2回目の修正）

前回の修正でも直りませんでした。原因を絞り込み直しました。

**原因**：`header a:hover` のように**要素名で絞っていた**こと。
テーマのヘッダーは `<header>` 要素ではないため、この指定が
そもそも一致していませんでした。

見出しや一覧のタイトル（`main` 配下）は効いていたので、
「サイト全体に効いている」と誤認していました。

**対処**：要素名に依存しない `a:hover` に変更しました。
子要素（`<span>`）まで届かせる点と、SVG アイコンを除外する点は前回のままです。

```css
a:hover,
a:hover *:not(svg):not(svg *) { color: var(--orita-accent) !important; }
```

**まだ直らない場合の調べ方**

1. メニューの文字を右クリック →「検証」
2. 開いたパネルで、その要素にマウスを乗せた状態の色を確認
3. 右側の「Styles」に、どのCSSが勝っているかが表示される

その画面のスクリーンショットを送っていただければ、原因を特定できます。

### 2026-08-04 (35回目) — 「学位」を「経歴」に統合

「学位」の節をなくし、3件を「経歴」の中へ時系列で組み込みました（計9項目）。

```
2024.7 –          准教授                      新潟大学 理学部
2020.3 – 2024.6   助教                        新潟大学 理学部
2018.4 – 2020.2   日本学術振興会 特別研究員-PD   首都大学東京 大学院理学研究科
2017.8 – 2018.3   博士研究員                   國家理論科學研究中心 (NCTS) 數學組
2017.4 – 2017.7   博士研究員                   九州大学 マス・フォア・インダストリ研究所
2017.3            博士 (数理科学) 取得          東京大学 大学院数理科学研究科   ← 学位
2014.4 – 2017.3   日本学術振興会 特別研究員-DC1  東京大学 大学院数理科学研究科
2014.3            修士 (数理科学) 取得          東京大学 大学院数理科学研究科   ← 学位
2012.3            学士 (理学) 取得              九州大学 理学部                ← 学位
```

**判断した点**

- 並び順は新しい順。学位は取得月（3月）の位置に挿入しました。
  博士 (2017.3) は DC1 の任期末にあたるため、その直前に入ります。
- 学位の行は「取得」を付けて、職位と区別できるようにしました。
- 学位の所属にもリンクを付け、他の行と体裁を揃えています。
- 表の見出しは CSS で非表示ですが、内部的には「職位・学位」に変更しました。

**英語ページの見出し**

`Experience` → **`Education and positions`** に変更しました。
学位が入ったため `Experience` だけでは実態と合わないためです。
別の表現がよければ言ってください（`Career` / `Curriculum vitae` など）。

### 2026-08-04 (36回目) — 残っていたホバー色（言語切替・検索・ダークモード・フッター）

**原因は2つありました。**

**1. SVG を除外していた**

arXiv と DOI の公式ロゴの色を守るため、ホバー色の対象から SVG を
除外していました。ところが検索・言語切替・ダークモードのアイコンも
SVG なので、まとめて弾かれていました。

調べたところ、arXiv と DOI のロゴは固有の色（`#b31b1b` など）を
直接持っており `currentColor` を使っていません。つまり
**文字色を変えても影響を受けません。** 除外は不要でした。

**2. `<a>` 以外の要素だった**

検索・言語切替・ダークモードは `<button>` や `<summary>` の可能性があります。
`a:hover` だけでは届きません。対象に `button` `summary` `[role="button"]` を
追加しました。

**あわせて見つかった潜在的な不具合**

`.prose a`（平常時の色）が `a:hover`（ホバー色）より**後**に書かれていたため、
同じ強さの指定として後勝ちになり、**本文中のリンクはホバーしても
色が変わっていませんでした。** 順序を組み替えて修正しています。

CSSの各ブロックに「(1)平常時 →(2)下線 →(3)ホバー →(4)アイコンの例外」と
順序の意味を明記したので、今後の手直しで同じ事故は起きにくいはずです。

### 2026-08-04 (37回目) — 見出しの変更

日本語ページの「経歴」→「略歴」。

### 2026-08-04 (38回目) — 訪問済みリンクの色

**症状**：フッターの「Hugo」が紫色になる。

**原因**：ブラウザ既定の「訪問済みリンク = 紫」でした。
テーマもこのリンクに色を指定しておらず、私の指定も
未訪問時とホバー時しか想定していなかったため、
一度クリックしたあとの状態が素通しになっていました。

**対処**：

```css
:where(a:visited) { color: inherit; }
```

`:where()` で書くと指定の強さがゼロになるため、
**ブラウザ既定だけを打ち消し、テーマの色指定には影響しません。**
あわせて、訪問済みリンクのホバーも確実に拾うようにしました。

### 2026-08-04 (39回目) — フッターのホバー色（指定の強さを最大に）

**症状**：フッターの「Hugo」「Blowfish」だけ、マウスオーバーで
テーマ既定の明るい青緑のまま。

**原因**：CSS は「より具体的に書かれた指定が勝つ」という規則があり、
私の `a:hover` はテーマ側の指定に負けていました。
`!important` を付けても、相手も同格の場合は具体性で決まります。

**対処**：`:not(#no-such-id)` を挟んで、指定の強さを最大にしました。

```css
a:not(#no-such-id):hover { color: var(--orita-accent) !important; }
```

そんな id はサイト内に存在しないので**対象は何も変わりません。**
CSS では id を含む指定が最も強いため、これでテーマ側のどんな指定にも
負けなくなります。見慣れない書き方ですが、こういう場面の定石です。

アイコンだけのリンク（YouTube・記事など）が巻き添えで色付かないよう、
そちらの例外指定にも同じ処理を重ねてあります。

### 2026-08-04 (40回目) — フッターのホバー色（検証パネルで原因確定）

ブラウザの検証パネルで、該当箇所のHTMLが判明しました。

```html
<a class="hover:underline hover:decoration-primary-400 hover:text-primary-500"
   href="https://gohugo.io/">Hugo</a>
```

テーマは Tailwind のクラスで
**文字色（`hover:text-primary-500`）と下線の色（`hover:decoration-primary-400`）を
別々に**指定していました。私は文字色しか上書きしていなかったため、
下線の色は明るいまま残ります。

**対処**：ホバー時に `text-decoration-color` も揃えるようにしました。
39回目で入れた「指定の強さを最大にする書き方」と合わせて、
これで文字色・下線色の両方がアクセント色になります。

なお検証パネルからは、テーマのCSSが `@layer base` / `@layer utilities` という
仕組みで書かれていることも分かりました。
`!important` を付けた指定はこの仕組みより優先されるため、
今回の書き方で確実に効きます。

### 2026-08-04 (41回目) — ヘッダーを明朝体に

サイト名（左上）とメニューを明朝体（Zen Old Mincho）にしました。
見出しと同じ書体になり、上部の印象が揃います。
本文とフッターはゴシックのままです（長文の読みやすさを優先）。

**あわせて調整**

- フォントの読み込みに 400・500 の太さを追加。
  メニューの文字は太字ではないため、これがないと擬似的に細らせた
  不自然な字形になります。
- メニューは文字が小さいので、字間を 0.04em 空けて締めています。

**ヘッダーの狙い方について**

Blowfish のヘッダーは `<header>` 要素ではなく
`<div class="fixed inset-x-0 z-100">` です（検証パネルで確認）。
id が付いていないため、class の組み合わせで指定しています。

```css
body > div[class~="fixed"][class~="inset-x-0"] { font-family: ...; }
```

テーマを更新して class が変わった場合、**明朝体が外れてゴシックに戻るだけ**で、
表示が壊れることはありません。その場合は検証パネルでヘッダーの
class を確認し、この行を書き換えてください。

### 2026-08-04 (42回目) — ヘッダーの明朝体を確実に

英語ページのヘッダーが明朝体になっていないとのご指摘。
ヘッダーの構造は日英で同じなので、キャッシュの可能性が高いのですが、
念のため指定を2点強化しました。

1. **狙い方を二重に**
   `class` の組み合わせ指定に加え、`fixed` を含む body 直下の div から
   検索欄（`#search-wrapper`）と先頭アンカー（`#the-top`）を除く指定を追加。
   どちらか一方が効かなくなっても、もう一方で拾えます。

2. **子孫要素まで届かせる**
   `font-family: inherit` を子孫に指定。テーマがメニューの `<span>` などに
   独自のフォントを指定していても上書きできます。

**それでも変わらない場合**：英語ページを開いた状態で Ctrl+F5。
CSSはブラウザに強く記憶されるため、ページごとに再読み込みが要る場合があります。

### 2026-08-04 (43回目) — iPhone での2つの問題

**1. 日付が電話番号としてリンクされる**

iOS Safari は本文中の数字の並びを電話番号・日付・住所と判定して
勝手にリンクにします。略歴の「2017.8 – 2018.3」が電話番号扱いされていました。

`layouts/partials/extend-head.html` を新設し、この自動判定を無効にしました。

```html
<meta name="format-detection" content="telephone=no, date=no, address=no">
```

万一リンク化されても本文と同じ見た目になるよう、CSS でも押さえてあります。

**2. 狭い画面で所属名が途中で切れる**

「國家理論科學研究中心 (NCTS) 數學組」が「國家理論 / 數學組」、
「九州大学 マス・フォア・インダストリ研究所」が「九州大学 / ストリ研」のように
中間が読めなくなっていました。

原因は、**和文を語の途中で折らない設定（`word-break: keep-all`）と
列の最小幅（`min-width`）** です。パソコンでの見栄えのために入れた設定が、
画面幅の狭いスマートフォンでは逆効果になり、長い所属名が
はみ出して切れていました。

640px 以下の画面では、この2つを解除して折り返しを許すようにしました。
パソコンでの見え方は変わりません。

### 2026-08-04 (44回目) — 一覧のリンク・背景速度・略歴の整理

**1. 論文一覧に全リンクを表示**

これまで arXiv だけだったところに、DOI と出版社ページも並べました。
リンクの種類で見せ方を変えています。

| 条件 | 表示 |
|---|---|
| name が arXiv | arXiv の公式ロゴ |
| リンク先が doi.org | DOI の公式ロゴ |
| それ以外 | name をそのまま文字で（枠付き） |

「それ以外」に該当するのは、京大数理研の講究録（日英）と
培風館の書籍ページの計3件です。DOI ではないので DOI のロゴは付けていません。

**2. 背景の速度を 1/4**

7.5秒 → **30秒**（1周期あたり）。
`assets/css/custom.css` と `assets/img/background.svg` の両方を揃えてあります。

**3. 略歴から1件削除**

「2017.4 – 2017.7 博士研究員 九州大学 マス・フォア・インダストリ研究所」を
日英とも削除。8項目になりました。

**4. 略歴の所属を大学名のみに**

| 変更前 | 変更後 |
|---|---|
| 新潟大学 理学部 | 新潟大学 |
| 首都大学東京 大学院理学研究科 | 首都大学東京 |
| 國家理論科學研究中心 (NCTS) 數學組 | 國家理論科學研究中心 (NCTS) |
| 東京大学 大学院数理科学研究科 | 東京大学 |
| 九州大学 理学部 | 九州大学 |

英語ページはもともと大学名のみだったため、変更はありません。

### 2026-08-04 (45回目) — 日本語版の概要を翻訳

日本語版の論文ページで英文のままだった概要11本を，日本語（ですます調）に
翻訳しました。

| 状態 | 本数 |
|---|---|
| もともと日本語（01・03） | 2 |
| 今回翻訳 | 11 |
| 概要なし（09 の教科書） | 1 |

**方針**

- **数式は原文のまま。** `$...$` の中身は一切変更していません。
  機械的に照合し、二重バックスラッシュが混入していないことも確認済みです。
- **用語は著者の既存の表記に合わせました。** 03 の概要で使われている
  「トーラス状単調 (toroidally monotone)」に倣い、
  spherically monotone は「球面状単調」としています。
- 定訳の定まっていない語（fragmentation ノルム、pseudoheavy、superheavy、
  boundary depth、topological complexity など）は英語のまま残しました。
- front matter の `abstract` も日本語に更新し、本文と一致させています。
- **英語版のページは変更していません。**

**確認のお願い**

翻訳は私が行ったものです。専門用語の選び方（概アーベル群、非変位可能、
相対単純群、非簡約 (unreduced) など）にご意見があれば教えてください。
該当箇所だけ差し替えます。

### 2026-08-04 (46回目) — 訳語の修正

ご指定の3語に統一しました。

| 原語 | 訳 |
|---|---|
| virtually abelian group | virtually abelian群 |
| non-displaceable | 非交叉配置不可能 |
| relatively simple group | relatively simple群 |

**補足**：作業前のファイルを確認したところ、`non-displaceable` が
「非交叉配置**可能**」（＝displaceable の意味）になっていました。
「不」が抜けて意味が逆になっていたため、07 と 08 の計4箇所を修正しています。

08 の冒頭は「変位可能性・非交叉配置可能性」という混在した状態だったので、
「可積分系のファイバーが非交叉配置可能かどうかは」と書き換えました。

### 2026-08-04 (47回目) — ページを開くたびに配色と背景が変わる仕組み

ご要望により、表示のたびに6種類から1つをランダムに選ぶようにしました。
配色だけでなく、**背景の模様と流れる向きも色ごとに変わります。**

| 配色 | 模様 | 流れる向き | 周期 |
|---|---|---|---|
| 青系 | 斜めの丸バー | 右上→左下 | 30秒 |
| 赤系 | 太い斜めストライプ | 左上→右下 | 36秒 |
| 紫系 | 大きなリング | 上へ | 44秒 |
| 水色系 | 水玉 | 下へ | 26秒 |
| トリコロール系 | 縦の帯（青・白・赤） | 右へ | 34秒 |
| ミッフィー風原色系 | 丸・四角・三角（赤青黄緑） | 右上→左下 | 40秒 |

**仕組み**

- 模様のSVGは**色を持ちません**（`static/img/pattern-*.svg`）。
  CSS のマスクとして形だけを使い、色は塗りで与えています。
  だから模様1枚で何色にも化けます。
- 配色は `layouts/partials/extend-head.html` の JavaScript が、
  読み込み時に CSS 変数（テーマの `--color-primary-*` と自前の
  `--orita-*`）を上書きして決めています。
- 6種類の向きはすべて「タイル1枚分ちょうど」動くので、継ぎ目なくループします。

**やめたいとき / 変えたいとき**

- **全部やめる**：`layouts/partials/extend-head.html` を削除すれば、
  緑系の固定配色に戻ります。本体には手を入れていません。
  （※ iOS の電話番号誤認を防ぐ meta タグも同じファイルにあるので、
   その部分だけは残してください）
- **一部だけ使う**：ファイル内の `PAL` 配列から不要な行を消します。
- **どの配色が出ているか調べる**：開発者ツールで `<html>` の
  `data-orita-palette` 属性を見ると名前が入っています。

**残したもの**

顔写真とファビコン・共有画像は緑のままです。配色によっては色が合いませんが、
ご指定のとおり据え置いています。

### 2026-08-04 (48回目) — 配色を8種類に（虹の七色＋ミッフィー）

配色を8種類に組み直し、模様と流れる向きも色ごとに変えました。

| 配色 | 模様 | 流れる向き | 周期 |
|---|---|---|---|
| 赤系 | 太い斜めストライプ | 左上→右下 | 36秒 |
| 橙系 | 波 | 右へ | 30秒 |
| 黄系 | 水玉 | 下へ | 26秒 |
| 緑系 | 斜めの丸バー | 右上→左下 | 30秒 |
| 青系 | 格子 | 左へ | 40秒 |
| 藍系 | 大きなリング | 上へ | 44秒 |
| 紫系 | 縦の帯 | 右へ | 34秒 |
| ミッフィー風原色系 | 丸・四角・三角 | 右上→左下 | 40秒 |

**緑系は現在の色をそのまま**使っています（`#2f7d63` / `#a9d5bd`）。
見慣れた配色が8回に1回そのまま出ます。

**ミッフィー風原色系**は、いただいた画像から6色を読み取って使っています。

```
#88593b 茶   #ea5416 朱   #fcdf03 黄
#736f61 灰   #175192 青   #1e7a21 緑
```

背景はこの6色の帯が斜めに並び、その上を丸・四角・三角が流れます。

**黄系について**：黄色は明るすぎて文字色に使うと読めなくなるため、
差し色とリンク色は落ち着いた黄土色寄りに調整しています。
背景の水玉だけが明るい黄色です。

**新しく作った模様**：波（`pattern-waves.svg`）と格子（`pattern-grid.svg`）。
どちらもタイルとして継ぎ目なく敷き詰まることを確認済みです。

**やめたいとき**：`layouts/partials/extend-head.html` の `PAL` 配列から
不要な行を消してください。1行だけ残せばその色に固定されます。

### 2026-08-04 (49回目) — 配色が緑のままだった問題

**症状**：背景の模様は配色ごとに変わるのに、ページ全体の色は緑のまま。

**原因**：上書きしていたのが**差し色（primary）だけ**でした。
Blowfish の `forest` 配色は、**地の色（neutral）自体が緑がかっています。**
画面の大部分を占めるのはこの地の色なので、差し色だけ変えても
緑の印象が残ります。

**対処**：地の色（`--color-neutral-*`）と副色（`--color-secondary-*`）も
配色ごとに上書きするようにしました。

地の色は**明度を標準的な無彩色に揃え、色相だけを配色に寄せています。**
明度を変えないので、文字と地のコントラストは配色によらず一定です
（8色すべてで文字が読めることを確認済み）。

あわせて、上書きするすべての値に `!important` を付けました。
テーマ側の指定に確実に勝つためです。

### 2026-08-04 (50回目) — 赤・緑・青で背景が動いて見えなかった問題

**原因**：模様の線の向きと、動く向きが平行になっていました。
線に沿って動かしても、線は同じ位置に重なり続けるので、見た目が変わりません。

| 配色 | 模様の線の向き | 動く向き | 結果 |
|---|---|---|---|
| 赤系 | ストライプが ↘ | ↘（同じ） | **完全に静止** |
| 緑系 | 丸バーが ↗ | ↙（同じ軸） | ほぼ静止 |
| 青系 | 格子の横線が → | ←（同じ） | 横線が静止 |

**検証**：模様を1/4周期ぶん動かした画像と元の画像を画素単位で比較しました。

```
修正前   stripes + 右下   変化 0.0%   ← 数値上まったく動いていない
         bars    + 左下   変化 16.0%
         grid    + 左     変化 3.8%
```

**対処**：線の向きと交差する向きに割り当て直しました。

| 配色 | 模様 | 動く向き | 周期 | 変化率 |
|---|---|---|---|---|
| 赤系 | 太い斜めストライプ | ↓ 下へ | 36秒 | 29.3% |
| 橙系 | 波 | → 右へ | 30秒 | 15.4% |
| 黄系 | 水玉 | ↘ 右下へ | 26秒 | 6.9% |
| 緑系 | 斜めの丸バー | ↘ 右下へ | 30秒 | 22.4% |
| 青系 | 格子 | ↙ 左下へ | 40秒 | 8.4% |
| 藍系 | 大きなリング | ↑ 上へ | 44秒 | 13.2% |
| 紫系 | 縦の帯 | → 右へ | 34秒 | 26.0% |
| ミッフィー風原色系 | 丸・四角・三角 | ← 左へ | 40秒 | 10.3% |

8色すべてで動きが見えることを数値で確認済みです。

**緑系の向きが変わります**：以前ご指定いただいた「右上→左下」は、
丸バーの伸びる向きと同じ軸だったため、ほとんど動いて見えていませんでした。
バーを直角に横切る「右下へ」に変更しています。
元の向きに戻したい場合は、丸バーの傾きを逆（↘）にすれば、
「右上→左下」に流れつつ動きも見える形にできます。

### 2026-08-04 (51回目) — 模様がそもそも表示されなかった問題

**症状**：赤系・緑系・青系で背景の模様が出ない。

**原因（本命）**：模様のSVGファイル3つが、**XMLとして壊れていました。**

私の生成スクリプトが `<g>` タグに `fill` と `stroke` を二重に書いていました。

```xml
<g fill="#fff" stroke="none" stroke="#fff" stroke-linecap="round" fill="none">
                             ^^^^^^^^^^^^                        ^^^^^^^^^^^
```

XML では同じ属性を2回書くと**致命的なエラー**になります。
ブラウザは壊れたSVGを読み込まないため、模様が丸ごと消えていました。

壊れていたのは bars（緑系）・stripes（赤系）・rings（藍系）の3つです。
藍系はまだ表示される機会がなかっただけで、同じく壊れていました。

50回目で「向きが平行だから動いて見えない」と診断しましたが、
それ以前にファイルが読み込まれていませんでした。**誤診でした。**
（向きの修正自体は有効なので、そのまま残しています）

**あわせて修正：模様の濃さのばらつき**

測ってみると、模様ごとに濃さが4倍以上違いました。

```
修正前   vstripes 24.5%  stripes 17.3%  bars 16.8%  waves  9.7%
         rings     7.6%  shapes   5.3%  grid  5.2%  dots   3.5%
```

薄い4つ（水玉・格子・図形・リング）の線を太く・数を増やし、
10〜26% の範囲に揃えました。青系の格子は特に細かったので、
線の太さを2倍以上にしています。

**検証方法**

- 8ファイルすべてを XML パーサで解析し、エラーゼロを確認
- 模様ごとの平均濃度を測定
- 1/4周期ぶん動かした画像との差分を測定（15〜33%、すべて視認可能）

### 2026-08-04 (52回目) — 模様の割り当て変更

| 配色 | 模様 | 動く向き | 周期 |
|---|---|---|---|
| 赤系 | 太い斜めストライプ | ↓ 下へ | 36秒 |
| 橙系 | 波 | → 右へ | 30秒 |
| 黄系 | 格子 | ↙ 左下へ | 40秒 |
| 緑系 | 斜めの丸バー | ↙ 左下へ | 30秒 |
| 青系 | 水玉 | ↓ 下へ | 26秒 |
| 藍系 | 大きなリング | ↑ 上へ | 44秒 |
| 紫系 | 縦の帯 | → 右へ | 34秒 |
| ミッフィー風原色系 | 丸・四角・三角 | ← 左へ | 40秒 |

**丸バーの傾きを反転しました**

「左下へ流す」というご指定を満たすため、バーの向きを
左下→右上（↗）から**左上→右下（↘）**に変えています。

以前の傾きのままだと、左下へ流す方向がバーの伸びる方向と同じ軸になり、
動いても見た目が変わりません（50回目で確認した現象です）。
傾きを反転すると、左下への動きがバーを直角に横切るので、
**「右上から左下へ流れる」見た目と、動きの見えやすさが両立します。**

**検証**：8種類すべてについて XML の妥当性・模様の濃さ（10〜26%）・
動きの見え方（15〜33%）を確認済みです。

### 2026-08-04 (53回目) — 丸バーの向きを元に戻した

緑系の丸バーの傾きを元（左下→右上 ↗）に戻し、
流れる向きはご指定どおり「右上→左下（↙）」にしました。

**前回の私の説明は不正確でした**

52回目に「バーの軸と同じ向きに流すと動いて見えない」と書きましたが、
実際に計測すると変化は 16.0% あり、十分に見えます。

```
バー(↗) を各向きに動かしたときの変化
  ↙ 左下  16.0%    ← ご指定。十分見える
  ↘ 右下  22.4%
  ↓ 下    23.7%
  ← 左    22.5%
```

バーは有限の長さで両端が丸いため、軸に沿って動いても端が流れて見えます。
直角に横切る場合より控えめですが、静止しているわけではありません。

50回目に「ほぼ静止」と判定したのは、しきい値の置き方が雑だったためです。
本当に静止していたのは赤系のストライプ（変化 0.0%）だけでした。

**現在の割り当て**

| 配色 | 模様 | 動く向き | 周期 | 動きの変化率 |
|---|---|---|---|---|
| 赤系 | 太い斜めストライプ | ↓ 下へ | 36秒 | 29.3% |
| 橙系 | 波 | → 右へ | 30秒 | 15.4% |
| 黄系 | 格子 | ↙ 左下へ | 40秒 | 23.5% |
| 緑系 | 斜めの丸バー | ↙ 左下へ | 30秒 | 16.0% |
| 青系 | 水玉 | ↓ 下へ | 26秒 | 22.7% |
| 藍系 | 大きなリング | ↑ 上へ | 44秒 | 33.3% |
| 紫系 | 縦の帯 | → 右へ | 34秒 | 26.0% |
| ミッフィー風原色系 | 丸・四角・三角 | ← 左へ | 40秒 | 28.7% |

### 2026-08-04 (54回目) — ミッフィー風原色系を作り直し

**ご指摘**：特にダークモードがミッフィー風でない。

**原因**：他の7色と同じ作り方をしていました。
つまり「ほぼ黒の地に、色相だけオレンジを混ぜる」という組み立てです。
ところがブルーナの絵本は、**地の色そのものが鮮やかな原色**で、
そこに白と黒の輪郭が乗る構成です。暗い地では別物になります。

**対処**：この配色だけ、地の色を手で組み直しました。

| | 地 | 文字 | リンク | ホバー |
|---|---|---|---|---|
| ダーク | 青 `#005192` | 生成り白 | 黄 `#ffdf00` | 白 |
| ライト | 生成り白 | 黒 `#080000` | 青 `#005192` | 朱 `#ea5415` |

**色の選び方は計算で決めました**。いただいた8色それぞれを地にしたときの
文字とのコントラスト比を出し、本文に使える組み合わせを選んでいます。

```
地 青 #005192 + 白文字 → 比 7.96  本文OK   ← 採用（ダーク）
地 黄 #ffdf00 + 黒文字 → 比 15.65 本文OK   ← 明るい地の候補
地 朱 #ea5415 + 白文字 → 比 3.57  見出しのみ
地 緑 #1d7a21 + 黄文字 → 比 4.10  見出しのみ
```

**背景の帯**もいただいた8色すべてを使うようにし、この配色だけ
模様の濃さを上げています（0.30 → 0.50）。ブルーナは色が強く出る作風なので、
他の配色と同じ薄さでは印象が合わないためです。

濃さは配色ごとに指定できるようにしました（`o` と `oD` の値）。

**検証**：8配色すべてについて、地と本文のコントラスト比を計算し、
全部が基準（4.5以上）を満たすことを確認しています。

### 2026-08-04 (55回目) — ミッフィー風をブルーナの描き方に寄せた

**ご指摘**：まだミッフィーらしくない。

**原因**：模様を「形だけのマスク」として扱い、色は背景から透かして
見せる仕組みにしていました。半透明で重なるため、原色が濁って
オリーブ色や灰色に見えていました。

ブルーナの描き方は逆で、**平坦な原色を不透明で置き、太い黒の輪郭で囲む**
という構成です。透かした時点で別物になります。

**対処**：この配色だけ、**色を持った模様**を新しく作りました
（`static/img/pattern-miffy.svg`）。マスクを使わず、そのまま敷いています。

- 青地（`#005192`）の上に、黄・朱・緑・白・橙・茶の形を配置
- 各形に太さ9pxの黒（`#080000`）の輪郭
- 形は円・角丸四角・三角・四分円・かまぼこ形の5種類
- 数を絞って余白を残す（ブルーナは疎な構成）
- 色は均等に配分（最初はランダムに選んで茶色に偏りました）
- ダークモードでの濃さを 0.92 に（ほぼ不透明＝平塗りに見せるため）

**キャラクターは描いていません。** ミッフィーそのものは著作物なので、
平塗り・黒縁・単純な幾何形という「作風」だけを借りています。

**仕組みの拡張**：模様は2通りの扱いができるようになりました。

| 方式 | 使い方 | 対象 |
|---|---|---|
| マスク | 形だけ持ち、色はCSSが与える | 他の7配色 |
| 画像 | 色を持ったまま敷く | ミッフィー風 |

`p:"none"` にすると後者になります。

### 2026-08-04 (56回目) — ミッフィー風を実用に耐える形に

**症状**：ブルーナらしくはなったが、模様が主役になって文字が読みにくい。

**測って分かったこと**

実際の見え方を再現し、文字とのコントラストが基準（4.5）を下回る
面積の割合を測りました。

```
ダーク（青地・白文字）
  濃さ0.55  全色を使う          読みにくい面積 6.4%
  濃さ0.55  白と黄を除く        読みにくい面積 0.0%   ← 白と黄が原因
```

白と黄は明るすぎて、白文字と競います。**ブルーナ自身も、地の色によって
使う色を変えています**（青地の絵に白い形はほとんど出てきません）。

**対処**

1. **形を小さく、数を増やした**
   大きさ 145〜205 → **66〜104**、数 10 → **18個**。
   画面に占める割合は約18%で、背景として成立する範囲です。

2. **明暗で模様を作り分けた**

   | | 使う色 | 濃さ |
   |---|---|---|
   | ダーク（青地） | 朱・橙・緑・茶（4色） | 0.55 |
   | ライト（白地） | 黄・朱・緑・橙・茶（5色） | 0.45 |

3. **黒の輪郭を細く**（9px → 6px）。形が小さくなった分、太いと潰れるためです。

**検証結果**：両モードとも、読みにくい面積 **0.0%** を確認しました。

```
ダーク 青地+白文字   最悪のコントラスト 5.30   読みにくい面積 0.0%
ライト 白地+黒文字   最悪のコントラスト 4.5超  読みにくい面積 0.0%
```

### 2026-08-04 (57回目) — 明暗を閲覧者の端末設定に合わせる

```toml
autoSwitchAppearance = true
```

閲覧者の端末（OS）のライト/ダーク設定に自動で合わせるようにしました。
端末側が判定できない場合は `defaultAppearance`（ダーク）が使われます。
右上のボタンでの手動切り替えは従来どおり可能で、選んだ状態は記憶されます。

**8配色すべてが両モードに対応済み**なので、この変更で崩れる配色はありません。
各配色は地の色・文字色・リンク色をライト用とダーク用の2組持っており、
どちらでも文字が読めることを確認してあります。

### 2026-08-04 (58回目) — ミッフィー風を静止・不透明に作り直し

**これまでの問題**：半透明で敷いていたため、
黒の輪郭が地の色と混ざって紺や灰色になり、原色もパステルに濁っていました。
ブルーナの本質は**不透明な平塗り**なので、透かした時点で成立しません。

**対処**：この配色だけ、まったく別の作りにしました。

| | これまで | 今回 |
|---|---|---|
| 透明度 | 半透明（0.45〜0.55） | **不透明（1.0）** |
| 動き | 左へ流れる | **静止** |
| 配置 | 画面全体に敷き詰め | **左右の余白のみ**（中央27〜73%は空ける） |
| 繰り返し | 縦横 | **縦のみ**（幅は画面に合わせて伸縮） |

静止にしたことで、**文字が来る位置をあらかじめ空けておける**ようになりました。
これが決定的で、不透明にしても文字が読めます。

**検証**：中央30〜70%の領域に形の画素が1つも無いことを確認しました。
本文は常に平坦な地の色（青または生成り白）の上に乗ります。

**狭い画面での扱い**：横幅1100px以下では本文が画面いっぱいに広がり、
左右の余白が無くなります。その場合は模様を消して地の色だけにします。
ブルーナの絵本にも単色の見開きは多いので、これはこれで様式に合います。

**仕組みの整理**：配色ごとに指定できる項目が増えました。

| 項目 | 意味 |
|---|---|
| `ins` | 背景の広げ方（動く配色は画面より広く、静止は画面ぴったり） |
| `br` | 繰り返し方（縦横 / 縦のみ） |
| `bs` | 大きさ |
| `on` `onD` | 狭い画面での濃さ |

**あわせて直した不具合**：項目名 `s` を「秒数」と「大きさ」の両方に
使ってしまい、アニメーションの秒数に `100% auto` が入っていました。
秒数を `sec` に改名して解消しています。
また、静止の配色で存在しないアニメーション名（`orita-drift-none`）を
指定してしまう問題も直しました。

### 2026-08-04 (59回目) — 背景が消えた原因と、未使用コードの削除

**症状**：ミッフィー風以外の7配色で背景の模様が出ない。

**原因**：`custom.css` に「マスク2枚重ね」の仕組みが入っており、
その合成方法が `mask-composite: intersect`（重なり部分だけを残す）に
なっていました。

2枚目のマスクは既定値が `none`＝何もない領域です。
**「何もない領域」との重なり部分は空**なので、模様が丸ごと消えます。
ミッフィー風はマスク自体を使わない（`--orita-pattern: none`）ため、
影響を受けませんでした。

前回の点検で「未使用だが害はない」と判断しましたが、**誤りでした。**
`none` との intersect が空になることを見落としていました。

**対処**：2枚重ねの仕組みごと削除し、マスク1枚の構成に戻しました。
未使用だった `--orita-veil` と `--orita-mask-size` も除去しています。

**あわせて修正**：ミッフィー風のライトモードのホバー色が
朱色（コントラスト比 3.57）で基準を下回っていました。
ブルーナ8色それぞれの比を計算し、緑（5.34）に変更しています。
リンクが青なので、ホバーを緑にすることで区別もつきます。

**点検結果**

| 項目 | 結果 |
|---|---|
| TOML 8 / front matter 201 / SVG 15 / CSS 416行 | 構文すべてOK |
| CSS変数 15個 | 過不足なし |
| 模様7種の濃度（9.7〜25.6%）と動き（15.4〜33.3%） | 全て適正 |
| コントラスト 48通り（8配色×明暗×3要素） | 基準割れ 0件 |

### 2026-08-04 (60回目) — ミッフィー風がトップページ以外で本文と重なる問題

**症状**：論文一覧など、トップページ以外で形が本文にかぶる。

**原因**：**トップページしか想定していませんでした。**

Blowfish は本文の置き方が2通りあります。

| ページ | 本文の位置 |
|---|---|
| トップページ（プロフィール） | 中央揃え（横 27〜73%） |
| 一覧・記事ページ | 左寄せ（横 11〜62%） |

前回は「中央27〜73%を空ける」設計にしたため、左寄せのページでは
本文の真上に形が来ていました。

**対処**：**右側（横76%以降）にだけ形を置く**構成に変えました。
どちらのレイアウトでも本文は左〜中央にあるため、右端に寄せれば
すべてのページで重なりません。

左帯の形は削除し、右帯に10個だけ配置しています。

**検証**：両レイアウトの本文領域について、形の画素が1つも無いことを
画素単位で確認しました。

```
ダーク / 一覧（左寄せ 11〜62%）   本文域の形 0  OK
ダーク / トップ（中央 27〜73%）   本文域の形 0  OK
ライト / 一覧（左寄せ）           本文域の形 0  OK
ライト / トップ（中央）           本文域の形 0  OK
```

形の左端は横76.2%で、本文の右端（73%）より右にあります。

### 2026-08-04 (61回目) — サイト内の移動では配色を変えない

**ご要望**：ページを移るたびに配色が変わらないようにしたい。
別のサイトへ行ったあとに戻ってきたときだけ変えたい。

**前提**：ブラウザには「別のサイトへ行った」ことを直接知る手段がありません。
離脱したかどうかは分からず、戻ってきたときの**時間差**でしか判断できません。

**実装**：ページを離れた時刻を記録し、次に開いたときの間隔で判定します。

- 間隔が **30秒未満** → 前と同じ配色を使う（サイト内の移動とみなす）
- 間隔が **30秒以上** → 選び直す（他のサイトを経由したとみなす）
- タブを閉じて開き直した場合も選び直す

サイト内のページ移動は1秒未満なので配色は保たれ、
他のサイトを見て戻れば必ず数秒以上空くので変わります。

**検証**：判定の流れを時系列で模擬し、想定どおり動くことを確認しました。

```
トップ → 論文一覧 → 個別ページ → 講演一覧   すべて同じ配色を保持
45秒後に戻る（他サイト経由）                  選び直す
12秒後に戻る（別タブを見ただけ）              同じ配色を保持
タブを閉じて開き直す                        選び直す
```

**調整**：`layouts/partials/extend-head.html` の `GAP` の値（ミリ秒）です。

| 値 | 挙動 |
|---|---|
| `0` | 常に選び直す（これまでと同じ） |
| `30 * 1000` | 30秒。**現在の設定** |
| `10 * 60 * 1000` | 10分空いたら変える |
| 非常に大きな値 | タブを閉じるまで固定 |

**保存の仕組み**：`sessionStorage` を使っており、タブを閉じれば消えます。
閲覧者の端末に残り続けるものではありません。
保存が使えない環境（プライベートモードなど）では、
これまでどおり毎回選び直されます。

### 2026-08-04 (62回目) — ダークの本文が黄色だった問題と、控えめ文字の見落とし

**症状**：ミッフィー風のダークモードで、本文が黄色。

**原因**：私が組んだ階調の設計ミスです。
テーマは本文に `--color-neutral-300` を使うのですが、そこに黄色（`#ffe75c`）を
置いていました。「明るい方から順に並べる」ことだけ考え、
**どの段がどこに使われるかを見ていませんでした。**

**対処**：用途を確認したうえで階調を組み直しました。

| 段 | 用途 | 色 |
|---|---|---|
| `neutral` | ライトの地／ダークの見出し | 生成り白 |
| `neutral-300` | **ダークの本文** | 生成り白（旧: 黄色） |
| `neutral-400` | ダークの控えめ文字 | 淡い青 |
| `neutral-500` | ライトの控えめ文字 | 中間の青 |
| `neutral-700` | ライトの本文 | 濃い青 |
| `neutral-800` | ダークの地 | ブルーナの青 |
| `neutral-900` | ライトの見出し | 黒 |

リンクは黄色のまま（ご指定どおり）、ホバーは白です。

**あわせて見つかった見落とし**

これまでの点検では「本文・リンク・ホバー」の3要素しか見ていませんでした。
**「控えめ文字」（日付や補足）を検証項目に加えたところ、
黄系と緑系のライトモードが基準を下回っていました**（4.22 / 4.23）。

色相を保ったまま明度を下げ、4.73 に引き上げています。

**検証**：8配色 × 明暗 × 5要素 = **80通りすべてが基準（4.5）以上**。
最も低いのは橙系のライトの控えめ文字で 4.57 です。

### 2026-08-04 (63回目) — ミッフィー風の形を上部のみに

**症状**：長いページで、右端に形が縦一列に並んで壁紙のようになる。

**原因**：模様を縦に繰り返していたため、ページが長いほど
同じ配置が何度も現れていました。ブルーナの絵本は余白が主役なので、
敷き詰めると様式から外れます。

**対処**：**最初の1画面ぶんにだけ形を置き、繰り返しをやめました。**

- `background-repeat` を `repeat-y` → `no-repeat` に
- 下へ行くほど形を小さく・疎にして、自然に消えるように配置
  （上部 170〜185px → 下部 70〜80px）
- それより下は平坦な地の色（青／生成り白）だけ

絵本の表紙をめくったような構成になり、長いページほど落ち着きます。
繰り返しが無いので、規則的な列は原理的に生じません。

**検証**：両レイアウト（一覧の左寄せ・トップの中央揃え）の本文領域に、
形の画素が1つも無いことを確認しました。

```
ダーク / 一覧（左寄せ 11〜62%）   本文域の形 0  OK
ダーク / トップ（中央 27〜73%）   本文域の形 0  OK
ライト / 一覧・トップ             いずれも 0  OK
```

形の左端は横75.2%で、本文の右端（73%）より右にあります。

### 2026-08-04 (64回目) — 訪問済みリンクが緑になる不具合

**症状**：一覧ページで、一度開いた論文のタイトルだけ濃い緑になり、
ダークモードの青地に沈んで読みにくい。

**原因**：訪問済みリンクの指定が弱すぎました。

58回目に `:where(a:visited) { color: inherit; }` と書きましたが、
`:where()` は**指定の強さがゼロ**になる書き方です。
ブラウザ既定の紫は打ち消せても、**テーマ側が訪問済みに当てている色には
負けます**。ライトモードでも同じ緑が出ていました。

**対処**：id を含む書き方（`a:not(#no-such-id):visited`）に変え、
テーマ側の指定より強くしました。訪問済みでも未訪問と同じ見た目になります。

- 一覧のタイトル → 見出しの色を継ぐ（白／黒）
- 本文中のリンク → 本文リンクの色を保つ
- アイコンだけのリンク → 色を変えない

**あわせて検証：ダークモードのリンク色について**

青地の上で本文リンクに使える色を、ブルーナ8色すべてについて調べました。

```
色        地との比   本文との差   判定
黄 #ffdf00   6.10      1.04     ← 唯一使える
朱 #ea5415   2.23      2.84     地に沈む
橙 #ee7701   2.81      2.26     地に沈む
緑 #1d7a21   1.49      4.25     地に沈む
茶 #88593b   1.37      4.64     地に沈む
白 #ffffff   8.10      1.28     本文と見分けにくい
```

朱・橙・緑・茶は地に沈んで本文に使えません。
黄色は明るさの差は小さいものの、**色相が本文の生成り白とまったく違う**ため
実際には十分見分けられます。この配色では黄色が唯一の妥当な選択です。

### 2026-08-04 (65回目) — ホバー色が全配色で効いていなかった

**症状**：一覧のタイトルにマウスを乗せると、緑になって地に沈む。

**64回目の診断は誤りでした。** 訪問済みリンクではなく、
マウスオーバーの色でした。

**原因**：CSSを組み立てる箇所で、**`.dark` の `--orita-accent` にだけ
`!important` が抜けていました。**

```js
+ '.dark{--orita-accent:' + p.aD          ← ここだけ抜けていた
  + ';--orita-link:'      + p.lD + ' !important'
  + ';--orita-bar:'       + p.bD + ' !important'
```

`:root` 側には付いているため、同じ強さの指定として**先に書かれた
`:root`（ライト用の色）が勝ち**、ダークモードでもライト用のホバー色が
使われていました。

**影響範囲は8配色すべて**です。ダークモードでのホバー色の
コントラスト比は 1.4〜3.0 しかなく、どの配色でも文字が沈んでいました。

```
        修正前   修正後
赤系     1.82  →  7.92
橙系     2.22  →  8.73
黄系     2.55  →  9.66
緑系     2.95  →  9.03
青系     1.80  →  8.11
藍系     1.39  →  7.72
紫系     1.76  →  8.01
ミッフィー 1.49  →  8.10
```

**対処**：`!important` を追加。80通りの検証はすべて基準を満たしています。

なお64回目に入れた訪問済みリンクの指定は、それ自体は妥当なので残しています
（ブラウザ既定の紫を防ぐ役割があります）。

### 2026-08-04 (66回目) — ダークでホバーの変化が見えない

**症状**：ダークモードでマウスを乗せても色が変わらない。

**原因**：指定は届いていました。**色が近すぎたのが問題です。**

ミッフィー風のダークは、見出しが生成り白（`#fffdf5`）、
ホバーが白（`#ffffff`）でした。この2色のRGB空間での隔たりは**わずか10**。
コントラスト比は 6.33 → 8.10 と数値上は変わりますが、
**目には同じ色に見えます。**

ライトモードは黒→緑ではっきり変わるため、非対称になっていました。

**検証**：候補の色を「地との比」と「見出しとの隔たり」の両面で測りました。

```
色              地との比  見出しとの隔たり  判定
白 #ffffff        8.10        10      見出しと似すぎ ← 従来
黄 #ffdf00        6.10       247      見分けられる   ← 採用
橙 #ee7701        2.81       279      地に沈む
淡緑 #7fd18a      4.39       173      地に沈む
水色 #7ec8ff      4.47       140      地に沈む
```

地に沈まず、かつ見出しと区別できるのは黄色系だけでした。

**対処**：ミッフィー風のダークのホバー色を白 → 黄（`#ffdf00`）に変更。

他の7配色は淡い同系色（隔たり98〜118）で、もともと問題ありません。
**8配色すべてで、地に沈まず見出しとも区別できることを確認済み**です。

**今回の学び**：これまでコントラスト比だけを見ていましたが、
**「変化が見えるか」は別の指標が要ります。**
比が高くても、元の色と近ければ変化として認識されません。
以後は「地との比」と「元の色との隔たり」の両方で判定します。

### 2026-08-04 (67回目) — アイコンのホバーと、略歴のリンクの目立ちすぎ

**症状1**：YouTubeアイコンにマウスを乗せても黄色にならず、暗くなる。

**原因**：「アイコンだけのリンクは色を変えず、薄くするだけ」という例外を、
**すべてのアイコンに一律で適用していました。**

この例外は arXiv と DOI の公式ロゴの色を守るために書いたものです。
ところがアイコンには2種類あります。

| アイコン | 色の持ち方 | 本来の扱い |
|---|---|---|
| arXiv / DOI | 固有の色（`#b31b1b` など） | 色を変えない |
| YouTube / 記事 / メール | `currentColor`（文字色に追従） | 他のリンクと同じでよい |

自作アイコンまで例外にしていたため、ホバーで色が変わらず
「薄くなる」効果だけが残り、ダークでは暗く見えていました。

**対処**：`viewBox` の値で公式ロゴだけを狙い撃ちする形に変更。

```css
.prose a:has(svg[viewBox="0 0 17.732 24.269"]),   /* arXiv */
.prose a:has(svg[viewBox="0 0 130 130"])          /* DOI */
```

自作アイコンは他のリンクと同じように色が変わります（下線は引きません）。

**症状2**：略歴の表で、所属名のリンクが8行すべて色付き＋下線で光っている。

数値上は問題ありませんが、表の中で一斉に主張して落ち着きません。

**対処**：表の中のリンクは通常時は本文と同じ色にし、
**マウスを乗せたときだけ**アクセント色になるようにしました。
リンクであることは薄い下線で示しています。

### 2026-08-04 (68回目) — アイコンのホバー色

**ご要望**：ダークで YouTube・記事のアイコンが常時黄色。
通常は本文と同じ色、マウスを乗せたときだけ色を変えたい。

**対処**：自作アイコン（YouTube・記事・メール）は `currentColor` で
描いているため、周囲のリンク色をそのまま拾って常時色付きになっていました。
通常は本文と同じ色にし、ホバーでのみ色が変わるようにしました。

**その過程で見つかった問題**

ホバー色に既存のアクセント色を使ったところ、**7配色で変化が小さすぎました。**
アクセント色は本文色と近い淡い同系色のため、アイコンだと差が分かりません。

```
       アイコンのホバー時の変化（本文色との隔たり）
赤系    51    橙系 45    黄系 43    緑系 45
青系    44    藍系 44    紫系 35    （80以上が目安）
```

**対処**：アイコン専用のホバー色を新設し、配色ごとに
**「地に沈まず、本文色から最も離れる色」**を階調から自動で選びました。

```
       変化（改善後）
赤系   137    橙系 174    黄系 207    緑系 165
青系   117    藍系  87    紫系  91    ミッフィー 214
```

緑系のライトだけは68にとどまります。本文色と配色が同系のため
これ以上離せませんが、判別可能な水準です。

**アイコンの扱いの整理**

| 種類 | 通常時 | ホバー |
|---|---|---|
| arXiv・DOI（公式ロゴ） | 固有の色 | 薄くする |
| YouTube・記事・メール（自作） | 本文と同じ色 | 専用のアクセント色 |

**検証**：文字の読みやすさ80通り、アイコンのホバー16通り、いずれも基準内。

**ライトの本文の色**：ご指定により、テーマの青系のまま据え置いています。

### 2026-08-04 (70回目) — ソースの総点検

**点検結果：不具合なし**

| 項目 | 結果 |
|---|---|
| TOML 8 / front matter 201 / SVG 15 / CSS 487行 | 構文すべてOK |
| JavaScript の括弧・引用符 | OK |
| 配色8種の設定項目 | 過不足なし |
| CSS変数 16個 | 未供給・余分ともなし |
| 模様9ファイル | すべて使用中・未使用ゼロ |
| `@keyframes` と使用する向き | 対応OK |
| 文字の読みやすさ 80通り | 基準割れ0（最低 4.57） |
| ホバーの変化 32通り | 基準割れ0（最低 68） |
| 背景の濃度（9.7〜25.6%）と動き（15.4〜33.3%） | すべて適正 |
| ミッフィーの形と本文域の重なり | 4通りとも0 |
| 日英の対応（論文15・講演41） | 一致 |
| 数式の二重バックスラッシュ | 0件 |
| 日本語版に残る英文の概要 | 0件 |
| メールアドレスの露出 | なし |

**整理したもの**

- `assets/img/background.svg`（43KB）を配布物から除外。
  トップページの背景方式を変えた際に使わなくなり、
  コメントアウトされた設定でのみ参照されていました。
- `params.toml` の `[homepage]` にあった古いコメントを実態に合わせて更新。

**誤検知だったもの**（記録として）

- 「未参照の模様7件」→ スクリプトが `p:"stripes"` などから
  ファイル名を組み立てるため、単純な文字列検索では追えないだけ。
  組み立て後の名前を再現して確認したところ、9ファイルすべて使用中。
- 「未参照の `_redirects`」→ Netlify が直接読むファイル。
- 「未参照の `email.svg`」→ 設定の `email` キーから名前が組み立てられる。
- 「`@keyframes` の未使用2件（dr・left）」→ 配色の割り当て変更で
  使わなくなったもの。他の向きに変更する際に必要なので残す。

### 2026-08-04 (71回目) — 訪問済みリンクが黒くなる問題（3度目の修正）

**症状**：ライトモードで、一度開いた一覧タイトルが黒くなる。

**これまでの誤り**

| 回 | 書いた内容 | なぜ誤りか |
|---|---|---|
| 64 | `color: inherit` | 親（見出し）の色を継ぐ |
| 69 | `color: unset` | **`color` は継承する性質を持つため、`unset` は `inherit` と同じ挙動になる** |

`unset` は「継承する性質のものは `inherit`、そうでないものは `initial`」
という仕様です。`color` は前者なので、69回目の修正は
実質何も変えていませんでした。

**正しい対処**：訪問済みリンクに、**未訪問と同じ色を明示的に指定する**。

テーマは一覧タイトルに `primary` 系の色を使っているので、同じ系統の色を
`:visited` にも与えます。ただし段を固定すると配色によって地に沈むため
（黄系ライト 2.87、緑系ライト 3.78、藍系ダーク 4.38、ミッフィーダーク 3.00）、
**配色ごとにコントラストが足りる段を自動で選び**、
`--orita-title` として渡す方式にしました。

```
       ライト        ダーク
赤系    p700 6.97    p400 5.19
橙系    p800 6.35    p400 7.11
黄系    p900 5.45    p400 9.79
緑系    p800 5.28    p400 8.04
青系    p700 7.06    p400 5.49
藍系    p700 10.16   p300 7.06
紫系    p700 7.34    p400 5.25
ミッフィー p700 6.33    p200 5.26
```

8配色×明暗の16通りすべてが基準（4.5）以上です。

### 2026-08-04 (72回目) — 訪問済みリンクの問題を根本から解決

**症状**：71回目の修正で黒は直ったが、今度はオレンジになった。

**原因**：「テーマは一覧タイトルに primary 系を使っている」という
**私の推測が外れていました。** 実際は secondary 系（青）です。
ミッフィー配色の primary は朱色なので、それがそのまま出ていました。

**根本の誤り**

64回目からの一連の修正は、すべて
**「未訪問時の色を、こちらで再現しよう」**としたものでした。
テーマがどの色を使うかは分かりませんし、更新で変わりえます。
この方針自体が誤りでした。

**改めて調べた結果：特別な指定は不要でした**

ブラウザ既定の「訪問済み＝紫」は、ユーザーエージェント原点の指定で、
サイト側の指定より必ず弱くなります。つまり紫が出るのは
**「テーマもこちらも色を指定していないリンク」だけ**です。

このサイトでは

- 一覧のタイトル・メニュー・フッター → テーマが色を指定している
- 本文中のリンク → こちらで `--orita-link` を指定している

ため、紫が出る箇所はもともと存在しません。

**対処**：64〜71回目に入れた指定をすべて削除しました。
何も指定しないのが正解でした。

```
64回目  inherit              → 親（見出し）の黒を継ぐ
69回目  unset                → color は継承性のため inherit と同じ
71回目  primary の段を指定   → テーマは secondary 系だった
72回目  指定を削除           → テーマ本来の色がそのまま出る
```

**あわせて整理**：不要になった変数 `--orita-title` と、
配色データの `tL` / `tD` を除去しました。

**反省**：64回目に「紫を打ち消す必要がある」と判断したこと自体が
誤りでした。実際に起きていたのはホバーの問題です。
誤った前提に基づく修正を3回重ね、そのたびに別の不具合を生みました。
**推測でテーマの内部仕様を決めつけない**ことが教訓です。

### 2026-08-04 (73回目) — リンク指定の重複と順序を整理

**症状**：ダークモードで略歴の表のリンクが再び黄色になった。

**原因**：67回目に入れた「表のリンクは控えめに」という指定は
残っていましたが、**書かれた位置が本文リンクの指定より前**でした。
CSS は同じ強さなら後に書いたほうが勝つため、効いていませんでした。

72回目の整理で本文リンクに `:link` を追加した際に、
この上下関係が逆転していたものです。

**さらに検査で判明**

CSS 全体の `color` 指定を機械的に洗い出したところ、
**リンク関連の指定がセクション2と5の2箇所に分散し、重複**していました。
表の指定より後ろに「自作アイコンは本文と同じ色」があり、
これも表の指定を上書きしていました。

**対処**：リンク関連をすべて1箇所に統合し、順序を明示しました。

```
(1) 本文中のリンクの色
(2) 例外: 公式ロゴ → 自作アイコン → 表の中
(3) 下線の扱い
(4) ホバー色（すべてに優先させたいので最後）
(4a,4b) ホバーの例外: 公式ロゴ / 自作アイコン
```

順序が正しいことを機械的に検算しています。

```
本文(240行) < アイコン(261) < 表(268) < ホバー(281) < アイコンホバー(307)
```

CSS は 491行 → 443行に減りました（重複の解消）。

**教訓**：同じ性質の指定を離れた場所に書くと、後の編集で順序が壊れます。
関連する指定は1箇所にまとめ、順序の意味をコメントに明記しました。

### 2026-08-04 (74回目) — 表のホバー漏れと、アイコンの色の見直し

**症状1**：表のリンクにマウスを乗せると、下線だけ色が変わり文字は変わらない。

**原因**：73回目の統合で、`.prose table a:hover` の指定を**書き漏らしました。**
通常時に `color: inherit !important` を掛けているため、
ホバー用の指定がないと文字色が戻りません。

**対処**：`(4c)` として表のホバー指定を追加。

**症状2**：YouTube アイコンのホバーが、期待した緑ではなくオレンジになる。

**原因**：アイコン専用のホバー色を「本文色から最も離れる色」という基準で
機械的に選んでいました。ミッフィー配色では primary（朱色）が選ばれます。

**調べ直した結果**

アクセント色をそのままアイコンに使えるか、8配色すべてで測りました。

```
配色        ダーク(比/変化)   ライト(比/変化)   判定
赤系         7.92/ 51 ★      8.49/ 72 OK     代替が必要
橙系         8.73/ 45 ★      6.65/ 75 OK     代替が必要
（中略）
ミッフィー   6.10/214 OK      5.34/ 89 OK     アクセント色で足りる
```

**ミッフィーだけはアクセント色で十分**でした（他の7配色はダークで
変化が35〜51と不足）。そこで「アクセント色で条件を満たす配色は
アクセント色を使い、足りない配色だけ代替を使う」方式に改めました。

結果、ミッフィーは**ライト＝緑、ダーク＝黄**（アクセント色と同じ）になります。

**検証**：アイコンのホバー16通り、すべて基準内。

**反省**：「本文色から最も離れる色を選ぶ」という機械的な規則を、
条件を満たす配色にまで適用していました。
**まず素直な選択を試し、駄目な場合だけ代替に回る**べきでした。

### 2026-08-04 (76回目) — ミッフィー風の3点修正

**1. 形が画面外にはみ出していた**

右上と右端の四角が途中で切れていました。
`background-size: 100% auto` で幅を画面に合わせているため、
形を右端まで置くと画面比によってはみ出します。

→ 右端に 108px の余白を確保し、形を x:75〜93% の範囲に収めました。

**2. ヘッダーの背景が肌色になっていた**

Blowfish のヘッダーは半透明（`fixed-fill-blur`）で、
**背後にある形の色を拾って濁っていました。**
地の色自体は白に近く、朱色の形が透けて肌色に見えていたものです。

→ 上端 180px には形を置かないようにしました（ヘッダーの高さは約150px）。

**3. ライトの地の色が温かすぎた**

`#fffdf5` は色相48°（黄）・彩度100%で、わずかですが黄みが強い色でした。
ブルーナの白はもっと中立です。

→ `#fdfdfb`（彩度33%）に変更。あわせて neutral-50〜300 も中立寄りに。

**検証**

```
形の範囲 x: 75%〜93%   右端の余白 108px   上端 180px
本文域（一覧・トップ、明暗）の形     4通りとも 0
ヘッダー域（上150px）の形           明暗とも 0
ライトの地/本文 コントラスト 10.76
ダークの地/本文 コントラスト  6.49
```

### 2026-08-04 (77回目) — 右端の空白を解消

**症状**：形が右端まで届かず、画面の右に空白の帯ができていた。

**原因**：76回目に「はみ出さないように」と右端に108pxの余白を取りましたが、
**これは慎重すぎました。**

`background-size: 100% auto` は、SVG の x 座標を画面幅に線形対応させます。
つまり **x が 1600（viewBox の幅）以下なら、画面幅がいくつでもはみ出しません。**
余白は不要でした。

**対処**：形を x:1215〜1592（76%〜100%）に配置し、右端まで使い切りました。
形の数も10個 → 12個に増やしています。

**満たしている条件**

| 条件 | 結果 |
|---|---|
| 本文の右端（73%）より右 | OK（76%から） |
| 画面外に出ない（x≤1600） | OK（最大1592） |
| ヘッダー（上150px）を避ける | OK（上端168） |
| 右端まで使う | OK（100%まで） |

本文域・ヘッダー域への形の侵入は、明暗×2レイアウトの
全4通りとも0です。

**反省**：76回目の「はみ出す」という診断は正しかったものの、
対処が過剰でした。**条件を計算で確かめずに余白を大きく取った**ため、
今度は空白が目立つ結果になっています。
まず条件を厳密に求め、必要最小限の対処にすべきでした。

### 2026-08-04 (78回目) — ヘッダーを不透明に

**症状**：ライトモードで、ヘッダーだけ肌色がかった帯になる。

**原因**：ヘッダーの設定が `fixed-fill-blur`（半透明＋ぼかし）でした。
背景の暖色の形をぼかして取り込むため、上部全体が濁って見えていました。
地の色自体は白（`#fdfdfb`）で問題ありません。

**対処**：`fixed-fill`（不透明）に変更しました。

```toml
[header]
  layout = "fixed-fill"
```

背景に色の強い形を置いている今の構成では、半透明のヘッダーとは
相性が良くありません。8配色すべてで上部が地の色に固定されます。

**検討した他の案**

| 案 | 判断 |
|---|---|
| ミッフィーのときだけ形を下げる | ぼかし半径は環境依存で、確実性に欠ける |
| ミッフィーのときだけヘッダーを不透明に | ヘッダーの class を狙う必要があり、テーマ更新で壊れやすい |
| 何もしない | 上部だけ色が浮いたまま |

**あわせて調整**：ヘッダーが不透明になったので、形をヘッダー直下
（上端162px）まで引き上げました。形の数も12個 → 13個に増やしています。

**失うもの**：スクロール時に背景が透ける演出はなくなります。

**検証**：本文域・ヘッダー域への形の侵入は、明暗×2レイアウトの
全4通りとも0です。

### 2026-08-04 (79回目) — ファビコンをミッフィー配色に

ブルーナの絵本表紙に多い「色面を黒い線で区切る」構成にしました。

```
黄 #ffdf00 │ 朱 #ea5415
──────────┼──────────
緑 #1d7a21 │ 橙 #ee7701
```

外枠と十字は黒（`#080000`）です。

**3案から選定**

| 案 | 16px での見え方 |
|---|---|
| A 2x2の色面 | 4色が判別できる ← 採用 |
| B 生成り白に円ひとつ | 円が潰れて点になる |
| C 黄地に白い円 | 黒枠が太く、中の白円が消える |

**小さいサイズは専用に描いています。** 大きい画像を縮小すると
黒い線が潰れて色面が濁るため、16px と 32px は線を細く
（辺の 5.5%、大きいサイズは 9%）して個別に生成しました。

**更新したファイル**

`favicon.ico` `favicon-16x16.png` `favicon-32x32.png`
`apple-touch-icon.png` `android-chrome-192x192.png` `android-chrome-512x512.png`

`site.webmanifest` の色も変更しました。

```
theme_color:      #0f1f1a → #ea5415（朱）
background_color: #0f1f1a → #fdfdfb（生成り白）
```

**注意**：ファビコンは配色ごとには変わりません。
8色のどれが表示されていても、常にミッフィー配色のままです。

### 2026-08-04 (80回目) — 共有画像もミッフィー配色に

SNS やメールにURLを貼ったときに出る画像（1200×630）を、
ファビコンと同じミッフィー配色にしました。

**構成**（サイトの画面と揃えています）

```
生成り白の地
  左  … 折田 龍馬（明朝体・黒）
        Ryuma ORITA（朱）
        新潟大学理学部 准教授
  右  … 平塗り + 黒縁の形を12個
```

見出しの書体は Noto Serif CJK JP で、サイトの Zen Old Mincho と
同系統の明朝体です。

**検証**：左半分（文字が入る領域）に形が1画素も無いことを確認しました。

**注意**：ファビコンと同じく、共有画像も配色ごとには変わりません。
常にミッフィー配色です。

### 2026-08-04 (81回目) — 最終の総点検

全項目を検査しました。**実害のある問題は0件**です。

**1. 構文** — すべてOK
TOML 8 / front matter 201 / SVG 14 / 画像 8（PNG・ICO）/
webmanifest / custom.css 456行（括弧一致・@import先頭）/
テンプレート3件 / JS 3ブロック（括弧・引用符）

**2. 整合** — すべてOK
配色8種（名前重複なし・項目22種の過不足なし）/
CSS変数16個（未供給・余分なし）/ 模様9ファイル全使用 /
@keyframes対応 / アイコンのviewBoxとCSSの対応 /
旧仕組みの残骸9種すべてゼロ

**3. 機能の存在** — 17項目すべてOK
配色保持（GAP30秒・離脱記録3系統・try/catch）/
電話番号誤認防止 / 狭い画面対応2種 / print・reduced-motion配慮 /
論文リンク3方式 / メール秘匿 / OS設定追従 / ヘッダー不透明 / 共有画像

**4. 実測** — すべて基準内
文字コントラスト80通り（最低4.57）/ ホバーの変化32通り（最低68）/
模様の濃度9.7〜25.6%・動き15.4〜33.3% /
ミッフィーの本文域・ヘッダー域への形の侵入 全6通りゼロ /
形の配置（左端76%・右端100%・上端162px）/
ファビコン5サイズ+ico（16/32/48収録）/ 共有画像の文字領域

**5. コンテンツ** — すべてOK
日英対応（論文15・講演41）/ lecture 86 / katex 29 /
数式チェック2種 / 英文残りなし / メール露出なし /
ですます調13件（残り2件は概要なしページで想定どおり）

**検査中の誤検知 3件**（実害なし・記録として）

1. 「メール秘匿の仕組みが無い」→ 検査条件が `createElement` を
   探していたが、実装は既存リンクを差し替える方式。露出ゼロを直接確認。
2. 「非交叉配置可能（不の抜け）2件」→ いずれも
   「〜かどうか」という問いの文脈で、46回目に意図的にそう書いた箇所。
   検査条件を「かどうか」を除外する形に改良し、真の抜けは0件。
3. 「ですます調が13/15」→ 残り2件は教科書（概要なし）と目次ページ。

**配布物**: 241ファイル / 637KB

### 2026-08-05 (82回目) — 英語版コンテンツを日本語版と同量に

英語版に不足していた **授業86ページ・ゼミのページ・メニュー2項目** を
日本語版から翻訳して追加しました。これで日英のページ数が揃います。

| セクション | 日本語 | 英語 |
|---|---|---|
| publication | 15 | 15 |
| talk | 41 | 41 |
| lecture | 86 | **86（新規）** |
| zemi | 1 | **1（新規）** |

**翻訳の方式**

本文の大半はURL・アイコンの定型です。全86ファイルから
**固有の日本語行393行（8,937字）を抽出し、行単位の対訳表**を作って
機械的に変換しました。URL・YouTubeアイコン・数式・インデントは
一切書き換えず、元のまま保存しています。

**検証**（すべて合格）

- ファイル数と名前: 日英で完全一致（86件）
- YAML・TOML: 全件解析OK
- URL集合: 日英で全ファイル一致（リンク切れ・書き損じゼロ）
- アイコン数・katex指定: 全ファイル一致
- 残存日本語: 書名など「(in Japanese)」注記付きのみ（意図どおり）
- 数式の二重バックスラッシュ: 0件

**方針**（ご指定どおり）

- リンク先は日本語のまま（シラバス・動画など）
- 日本語の書名は原題を残し「(in Japanese)」を付記、著者名はローマ字化
- メニューは Teaching（授業）/ Seminar（ゼミ）

**ご確認いただきたい点**

1. 「大学院総合学術研究科」の英名を **Graduate School of Integrated
   Studies** と訳しました（1ファイル）。公式英名をご確認ください。
2. 学位論文の学生名のローマ字表記（Kohei Yamada / Kanta Koeda /
   Kanon Yashiro / Yuki Yamamoto）をご確認ください。
3. メニュー名 Teaching / Seminar がお好みに合うか。

### 2026-08-05 (83回目) — 9言語のトップページを追加

英語・日本語に加えて、以下の9言語のトップページを作成しました。

| 言語 | コード | 表示 | 名前の表記 |
|---|---|---|---|
| フランス語 | fr | FR | Ryuma ORITA |
| ドイツ語 | de | DE | Ryuma ORITA |
| 簡体中国語 | zh-cn | 简 | 折田龙马 |
| 繁体中国語 | zh-tw | 繁 | 折田龍馬 |
| 韓国語 | ko | KO | 오리타 류마 |
| ロシア語 | ru | RU | Рюма Орита |
| ヘブライ語 | he | HE | ריומה אוריטה |
| ギリシャ語 | el | EL | Ριούμα Ορίτα |
| 古典ラテン語 | la | LA | Ryuma ORITA |

固有名詞は各言語の表記に合わせました。中国語は漢字がそのまま使えるため
簡体・繁体で字体を変えています。

**各ページの内容**（英語版と同じ構成）

- 自己紹介の文
- 研究興味 3項目
- 略歴の表 8行（リンク先は各大学の英語ページ）
- 「詳細ページは英語と日本語で提供」の一文

**メニュー**：各言語のラベルで、英語のページへ誘導します。
トップページだけの言語でも行き止まりにならないようにしました。

**ヘブライ語（RTL）への対応**

1. `rtl = true` を設定。
2. 略歴の日付を `<span dir="ltr">` で囲みました。RTLの文中では
   「2020.3 – 2024.6」が左右反転して表示されうるためです。
3. **CSSの2箇所を論理プロパティに変更しました。**
   検査で見つかったもので、そのままではヘブライ語で崩れます。

```
text-align: left    → text-align: start        （RTLでは右揃えになる）
padding-right       → padding-inline-end       （RTLでは左側の余白になる）
```

**ラテン語について**：新ラテン語の学術慣行に沿って
professor extraordinarius（准教授）、professor adiunctus（助教）、
Universitas Niigatensis などとしました。

**検証**：設定26ファイルのTOML解析 / コンテンツ297ファイルのYAML解析 /
weightの重複なし / contentDirと_index.mdの実在 / 全言語でリンク8本・
表8行 / ヘブライ語の日付8箇所すべてLTR保護 / RTLで崩れる指定ゼロ。

**注意**

- テーマ（Blowfish）が持つUI文言の翻訳に無い言語（ラテン語など）は、
  「検索」などの表示が英語になります。トップページのみなので影響は
  ほぼありません。
- 言語切替の一覧が11件になります。

### 2026-08-05 (84回目) — 職位の訳語を修正（ご指摘による）

**ご指摘**：中国語の「助教」は「助理教授」ではないか。

**そのとおりでした。**日本語の「助教」は2007年以降の教員職
（Assistant Professor）ですが、中国語の「助教」は
**ティーチング・アシスタントや最下位の講師職**を指します。
字が同じで意味が違う、いわゆる偽の友です。漢字をそのまま移していました。

**同種の誤りを他にも発見**

全言語の職位を横に並べて点検したところ、韓国語に同じ誤り、
ギリシャ語に階級の取り違えがありました。

| 言語 | 修正前 | 修正後 | 理由 |
|---|---|---|---|
| 簡体中国語 | 助教 | **助理教授** | 中国語の「助教」はTA・最下位講師職 |
| 繁体中国語 | 助教 | **助理教授** | 同上（台湾でも助理教授が正式階級） |
| 韓国語 | 조교 | **조교수** | 韓国語の「조교」は助手・TA |
| ギリシャ語 | Λέκτορας | **Επίκουρος καθηγητής** | Λέκτορας は1つ下の階級 |
| ドイツ語 | Wissenschaftlicher Assistent | **Assistenzprofessor** | 旧職名（2002年廃止）のため現行語に |

**残りの項目も点検**：JSPS特別研究員（PD/DC1）、博士研究員、
博士・修士・学士の各学位について全9言語を横並びで確認しました。
偽の友の問題はありません。

**教訓**：漢字圏の言語で、同じ字だからと機械的に移すのは危険。
**階級を表す語は、その言語の制度に照らして確認する**こと。

### 2026-08-05 (85回目) — 9言語の訳文の総点検

導入文・見出し・研究興味・略歴の全項目・注記・メニュー・設定ファイル
（headline / bio / description / 日付書式）を、9言語すべて横並びで
一語ずつ照合しました。

**修正した5件**

| 言語 | 修正 | 理由 |
|---|---|---|
| 繁体中国語 | 拓樸學・拓樸複雜度 → **拓撲**學・**拓撲**複雜度 | 台湾の正式な学術用語は「拓撲」（拓樸は俗用） |
| ドイツ語 | Hamiltonscher → **hamiltonscher** / Mathematischen → **mathematischen**（計5箇所） | 現行正書法では形容詞は小文字（人名由来の -sch も同様） |
| ギリシャ語 | Πρότυπα εμμονής → **επιμονής** | εμμονή は「執着」の含みが強い。persistence は επιμονή |
| ギリシャ語 | 学位名の形容詞を小文字化、学士に θετικών（理学）を明示（3箇所） | ギリシャ語の慣用 |
| ヘブライ語 | מרצה → **מרצה בכיר** | イスラエルの職階対応表で Assistant Professor は上級講師 |
| フランス語 | Maître de conférences → **Professeur adjoint** | 同一表内で仏国内制度と北米式が混在していたため北米式に統一 |

**点検して問題なしと判定した項目**

- 名前の転写（折田龙马 / 오리타 류마 / Рюма Орита / ריומה אוריטה / Ριούμα Ορίτα）
- 大学名9言語 × 5大学（東京都立大は在籍当時の名称の扱いを含む）
- JSPS特別研究員・博士研究員・学位3種の全言語
- 数学用語（辛几何・심플렉틱・симплектическая・συμπλεκτική 等）
- メニュー4項目 × 9言語 / 日付書式 / 注記の一文

**近似にとどまる訳語**（制度が対応しないため。記録として）

- 仏 Professeur associé / adjoint … 北米式。仏国内制度とは一対一でない
- 露 Ассистент … ロシアの職階での最も近い職
- 希 Επίκουρος καθηγητής / 伊 מרצה בכיר … 各国の対応表に基づく

### 2026-08-05 (86回目) — 中国語版の姓名を空ける（ご指摘による）

**ご指摘**：中国語版で姓と名の間にスペースがない。

**中国語圏の人名は詰めて書きます**（毛泽东、李政道）。しかし
**日本人の姓名は、中国語圏の報道や学術文献でも姓と名を空ける**のが
一般的です。日本人名は姓・名とも2字が多く、区切りがないと
どこで切れるか判別しにくいためです。

日本語版が「折田 龍馬」と空けているのに中国語版だけ詰めており、
**不統一でした。**

| 言語 | 修正前 | 修正後 |
|---|---|---|
| 簡体中国語 | 折田龙马 | **折田 龙马** |
| 繁体中国語 | 折田龍馬 | **折田 龍馬** |

計8箇所（各言語の設定ファイル3箇所＋原稿1箇所）。

これで日本語・簡体・繁体・韓国語の4言語すべてで姓名が空きます。

```
ja     折田 龍馬
zh-cn  折田 龙马
zh-tw  折田 龍馬
ko     오리타 류마
```

### 2026-08-05 (87回目) — 総点検（多言語化の完了確認）

前回の点検で「表記の統一」という軸が抜けていた反省をふまえ、
訳語の正しさに加えて**表記・構造・言語タグ**を軸に加えて検査しました。

**修正した4件**

| 対象 | 修正前 | 修正後 | 理由 |
|---|---|---|---|
| 簡体中国語 | isoCode = "zh-cn" | **"zh-CN"** | BCP47 では地域部分は大文字。HTMLのlang属性・hreflang・読み上げソフトが参照する |
| 繁体中国語 | isoCode = "zh-tw" | **"zh-TW"** | 同上 |
| 簡体中国語 | description の「理学部副教授」 | **「理学部 副教授」** | headline と空き方が不一致だった |
| 繁体中国語 | description の「理學部副教授」 | **「理學部 副教授」** | 同上 |

**問題なしと確認した項目**

- 構文: TOML 26 / front matter 297 / SVG 14 / 画像 8 / webmanifest / CSS 457行 / テンプレート3
- 構造: 9言語すべて同一（見出し2・研究興味3項目・略歴8行・リンク8本・注記1行）
- 表記: 姓名の空き11言語 / description のダッシュ11言語 / 日付の区切り44箇所すべて en dash
- リンク: 9言語すべて英語版と同一のURL集合
- メニュー: 11言語 × 4項目、URL・weight とも整合
- 配色: 文字コントラスト80通り（最低4.57）/ ホバーの変化32通り（最低68）
- RTL: 崩れる指定ゼロ（論理プロパティ化済み）
- 英語版授業86件: URL・アイコン・katex の崩れゼロ、未翻訳の日本語ゼロ
- 画像: ファビコン5サイズ + ico（16/32/48）/ 共有画像の文字領域

**配布物**: 355ファイル / 780KB

### 2026-08-05 (88回目) — スペイン語・ポルトガル語・イタリア語を追加

数学の主要言語を揃える目的で3言語を追加しました。合計14言語です。

| 言語 | コード | isoCode | 追加の理由 |
|---|---|---|---|
| スペイン語 | es | es | 話者数が多く、ICMAT（マドリード）など幾何の拠点がある |
| ポルトガル語（ブラジル） | pt-br | pt-BR | IMPA は力学系・幾何の世界的中心。研究分野と直結 |
| イタリア語 | it | it | SISSA・ピサなどシンプレクティック幾何の伝統が厚い |

**職位の訳語は各国の制度に照らして選定**（前回までの誤訳の反省をふまえ）

作成の途中で2件、制度上の不適合を見つけて修正しました。

- **スペイン語**: Profesor ayudante → **Profesor asistente**
  スペインで「ayudante」は博士号取得前の職を指すため、
  博士取得後の助教には不適切。
- **イタリア語**: Ricercatore → **Ricercatore universitario**
  次行の「Ricercatore JSPS (PD)」と紛らわしいため正式名称に。

**検証**（14言語すべて）

- 構造: 12言語すべて同一（見出し2・研究興味3項目・略歴8行・リンク8本・注記1行）
- isoCode: BCP47 に準拠（pt-BR の地域部分も大文字）
- title と author.name の一致 / description のダッシュ / 日付の区切り
- リンク集合が英語版と同一 / メニューのURL・weight
- 構文: 設定32ファイル・コンテンツ300ファイル

**配布物**: 364ファイル / 795KB

**ご確認いただきたい点**

スペイン語の「Profesor asociado」（准教授）は、**スペイン国内では
非常勤講師を指す語**です。中南米では准教授の意味で通じます。
スペインの同僚に見ていただく機会があれば、
「Profesor titular」への変更をご検討ください
（ただし中南米では教授の意味になりうるため、一長一短です）。

### 2026-08-05 (89回目) — 言語一覧の表記と並びを整理

**指摘された2つの問題**

1. **表記の体系が混在していた**

```
EN JA FR DE 簡 繁 KO RU HE EL LA
            ↑↑ ここだけ漢字1文字
```

他がラテン文字2文字なのに中国語だけ漢字で、しかも「簡」「繁」単独では
中国語だと分からない状態でした。

2. **並びに意味がなかった**

ご依頼いただいた順そのままで、閲覧者にとっての手がかりがありませんでした。

**対処**

`displayName` を**各言語の自称**に統一しました。国際化の実務では、
訪問者は自分の言語名を字面で見つけるため、これが標準的な作法です。

並びは「完全版の2言語を先頭 → 残りは言語コード順」としました。
コード順は文字体系に依存しないので、言語が増えても位置が安定します。

```
 1 English      8 Italiano
 2 日本語        9 한국어
 3 Deutsch     10 Latina
 4 Ελληνικά    11 Português
 5 Español     12 Русский
 6 Français    13 简体中文
 7 עברית       14 繁體中文
```

**検証**: weight は 1〜14 の連番で重複なし / displayName の重複なし /
TOML 32ファイル解析OK。

**トレードオフ**: ヘッダーの言語ボタンが「JA」から「日本語」になり、
少し横幅を取ります。短い表記に戻したい場合は各
`languages.<コード>.toml` の `displayName` を書き換えるだけです。

### 2026-08-05 (90回目) — ギリシャ語の字体を修正

**症状**：言語一覧で「Ελληνικά」のアクセントが文字から離れ、
「Ελληνικα´」のように見える。

**原因の切り分け**

まず文字データを疑い、Unicode で検査しました。

```
U+03AC  GREEK SMALL LETTER ALPHA WITH TONOS  ← アクセント付きα 1文字
NFC正規化しても変化なし = 元から正常
```

全14言語の displayName と全原稿を検査し、**結合文字（分解形）は
1つもありませんでした。** データは正しく、原因は**フォント**でした。

見出しに指定した Zen Old Mincho は日本語用のため、ギリシャ文字の
アクセント付き字を持ちません。次候補の Noto Serif JP も同様です。
結果、システムが別のフォントから合成し、字体が崩れていました。

**対処**：欧文セリフ体を候補の2番目に挟みました。

```css
font-family: "Zen Old Mincho", "Noto Serif", "Times New Roman",
             "Noto Serif JP", "Yu Mincho", Georgia, serif;
```

Noto Serif はギリシャ文字・キリル文字を含みます。日本語・中国語の字は
欧文フォントに無いので、これまでどおり Zen Old Mincho が使われます。

あわせて Noto Serif を読み込みに追加しました（@import は1回のまま）。

**補足**：ヘブライ語はアクセント記号を使わないため、この問題は
起きていません（実際の画面でも字体は自然でした）。

**あわせて訂正**：89回目に「ヘッダーのボタンが JA から 日本語 になる」と
書きましたが**誤りでした**。ヘッダーは言語コード（JA / KO / PT-BR）を
表示し、設定では変わりません。一覧だけが displayName を使います。
結果として「ヘッダーは短いコード、一覧は自称」という役割分担になり、
これが望ましい形です。

### 2026-08-05 (91回目) — ヘッダーの言語バッジについて（未解決・実害なし）

**現象**：ヘッダーの言語バッジが、言語によって表示規則が違う。

| 言語 | 設定した displayName | ヘッダーの表示 |
|---|---|---|
| 韓国語 | 한국어 | KO |
| イタリア語 | Italiano | IT |
| ギリシャ語 | Ελληνικά | EL |
| ポルトガル語 | Português | PT-BR |
| 簡体中国語 | 简体中文 | **简体中文** |

中国語だけ displayName が出て、他は言語コードの大文字になります。

**確認したこと**

- 設定ファイルは14言語すべて同じ形式（locale / isoCode / displayName）
- 原稿は14言語すべて実在し、サイズも正常
- **各言語のページ本文は、正しくその言語で表示されている**
  （ギリシャ語・イタリア語で実機確認済み）
- 言語一覧（プルダウン）は全言語が自称で正しく表示される

**判定：実害なし。対処しない。**

不一致はヘッダーの小さなバッジ1箇所のみ。「KO」でも「简体中文」でも
「いま何語を見ているか」は伝わり、一覧を開けば全言語が自称で並ぶ。

揃えるにはテーマの表示部分を上書きすることになるが、
仕組みを把握しないまま触れば更新時に壊れる箇所を増やすだけ。

**規則は特定できず**

テーマ本体は配布物に含まれない（Netlify側で取得）ため、
手元で確かめられない。心当たりとして、テーマが中国語を公式対応言語として
持っている（リポジトリに README.zh-cn.md がある）ことが関係する可能性が
あるが、未確認。

**この件での反省**

1. ヘッダーの挙動について**推測で2回続けて断定し、2回とも誤った**。
   「設定では変えられません」「ブラウザのキャッシュです」はいずれも
   確認していない推測だった。
2. 表示の細部を追う前に、**まずページが正しく生成されているかを
   確かめるべきだった**。順序が逆で、無用な確認をお願いしてしまった。

**教訓**：手元で再現できない事象は、推測を述べず「分からない」と言う。
切り分けは「実害の有無」から始める。

### 2026-08-05 (92回目) — 公開サイトの実物調査（バッジ問題の続き）

ヘッダーの言語バッジ問題を解決するため、方針を変えて
**公開中のサイトそのものをHTTPで取得して調査**しました。

**発見1：本番サイトは古い版のまま**

- `https://ryuma-orita.netlify.app/`（英語トップ）
  … Blowfish 化された**初期の版**。言語は EN/JA の2つだけ、
  メニューは Publications / Talks のみ、略歴は9行。
- `https://ryuma-orita.netlify.app/ja/`
  … **旧サイト（Academic テーマ）のまま**。生成器の記録が
  「Source Themes Academic 4.7.0」。電話番号・住所・
  問い合わせフォームがまだ公開されている。

つまり本番には今回の作業がまだ反映されておらず、
14言語版のスクリーンショットは**別のURL（お試しサイト）**のもの。

**発見2：本番の略歴には「九州大学 博士研究員」の行がある**

現行ファイル（全14言語・8行）に無い行が本番（9行）にあり、
一瞬「翻訳作業のどこかで経歴を1件落とした」と焦りましたが、
**過去の記録を確認**したところ、44回目に

> 「2017.4 – 2017.7 博士研究員 九州大学 マス・フォア・インダストリ研究所」を
> 日英とも削除。8項目になりました。

と、**ご本人の指示による削除**であることが記録されていました。
現行ファイルは正しい。**何も変更していません。**

推測で「欠落だ」と復元していたら、ご指示を無に帰すところでした。
**直す前に、過去の記録で意図を確認する**——この手順の価値が実証された形です。

**バッジ問題の次の一手**

お試しサイトのURLが分かれば、/ko/ /el/ /zh-cn/ の**実際のHTML**を
取得して、バッジ部分に何が書き出されているかを直接読めます。
推測ではなく実物で特定できます。

### 2026-08-05 (93回目) — ヘッダーの言語バッジ問題を解決

91回目に「規則が分からない・実害なし」とした問題を、実物の証拠で解決しました。

**決め手となった証拠**（ご提供いただいたもの）

1. バッジのHTML: `<span ... title="Ριούμα Ορίτα">EN</span>`
   → ギリシャ語の文脈で描画されているのに中身が EN。
   しかも「EN」という文字列は設定14ファイルのどこにも無い。
   → バッジは設定の外から来ている。
2. テーマの `i18n/en.yaml` の冒頭: `global: language: "EN"`
   → バッジの正体は **i18n（UI翻訳）の global.language**。

**全観測の説明**

| 言語 | テーマの翻訳 | その language | バッジ |
|---|---|---|---|
| 韓国語 | ko.yaml あり | "KO" | KO |
| イタリア語 | it.yaml あり | "IT" | IT |
| ポルトガル語 | pt-BR.yaml あり | "PT-BR" | PT-BR（設定がPTでもPT-BRと出た謎も解決） |
| 簡体中国語 | zh-CN.yaml あり | "简体中文"（翻訳者が言語名を記載） | 简体中文 |
| ギリシャ語 | **el.yaml なし** | → 英語版へ落ちる | **EN** |

displayName は一覧にのみ使われ、バッジは i18n から来ていた。
歴代のバッジが設定と無関係に「JA」だったのも同じ理由。

**対処**（テーマには一切触れない公式の拡張方法）

サイト側に `i18n/` フォルダを新設し、14言語分を配置。

- 12言語: `global.language` の1項目だけ上書き
  （EN / JA / FR / DE / ZH-CN / ZH-TW / KO / RU / HE / ES / PT-BR / IT）。
  Hugo は項目単位でサイト側を優先するので、UI文言はテーマの翻訳のまま。
  中国語もコード表記になり、**14言語のバッジが同一体系に揃う**。
- ギリシャ語・ラテン語: テーマに翻訳が存在しないため**全文（73項目）を新規作成**。
  バッジが EL / LA になるのに加え、「検索」「ページが見つかりません」などの
  **UI文言もギリシャ語・ラテン語になる**。
  85回目の注意書き「翻訳に無い言語はUIが英語のまま」も同時に解消。

**確認方法**（ローカル）: hugo server を再起動し、/el/ でバッジが EL、
/zh-cn/ で ZH-CN になれば成功。バッジを言語名（简体中文 等）に
したい場合は、各 i18n ファイルの language の値を変えるだけ。

**教訓**: 二度の誤推測の後、(1) 実物のHTML 1行、(2) テーマの翻訳ファイル、
という2つの証拠だけで確定できた。**動かぬ証拠を先に取りに行く**こと。

### 2026-08-05 (94回目) — バッジ修正の実機確認

ローカルの実機で確認いただきました。

- ギリシャ語版のヘッダー: EN → **EL** に修正された
- 簡体中国語版のヘッダー: 简体中文 → **ZH-CN** に統一された

これで91回目からの「ヘッダーの言語バッジ問題」は**解決・確認済み**です。
ヘブライ語（HE）・ラテン語（LA）のバッジも実機で確認済み。**14言語すべて正常**です。

### 2026-08-05 (95回目) — 韓国語・中国語の見出しを明朝体系に

**ご要望**：日本語版で明朝体の箇所を、韓国語版でも明朝体系にしたい。

**原因**：見出しのフォント指定に日本語用（Zen Old Mincho）と欧文用しか
なく、**ハングルはどのフォントにも該当せず、システムのゴシック体に
落ちていました**。

**同じ型の問題が中国語にも**：簡体字の「龙」（トップの名前！）などは
Zen Old Mincho に無いため、見出しの中で明朝体とゴシック体が
混ざりえます。繁体字にも同様の穴があります。3言語まとめて対処しました。

**対処**：言語ごとに「その言語の明朝体」を変数で切り替える方式。

```css
:root        { --orita-serif-local: "Zen Old Mincho"; }  /* 既定=日本語 */
:lang(ko)    { --orita-serif-local: "Noto Serif KR"; }   /* 명조체 */
:lang(zh-CN) { --orita-serif-local: "Noto Serif SC"; }   /* 宋体 */
:lang(zh-TW) { --orita-serif-local: "Noto Serif TC"; }   /* 明體 */

font-family: var(--orita-serif-local, "Zen Old Mincho"),
             "Zen Old Mincho", "Noto Serif", ...;        /* 先頭に差し込み */
```

- 判定は html の lang 属性（isoCode）。93回目で isoCode を BCP47 に
  正した（zh-CN 等）ことがここで効いています。
- 中国語は言語全体を SC / TC で描くため、日本の字体と混ざりません
  （簡体は宋体、繁体は明體の正しい字形になります）。
- **日本語・他言語には影響なし**（既定値が従来どおり）。

**読み込み**：Noto Serif KR / SC / TC を @import に追加（1回のまま）。
Google Fonts は CJK フォントを使用文字の小分けで配信するため、
実際に取得されるのはトップページで使う文字の分だけです。

**検証**：CSS括弧一致 / @import 1回・先頭 / 変数の供給4・使用1 /
設定32・i18n 14 解析OK。

**注意**：検証スクリプトの「CSS変数の未供給」検査は --orita-accent,
--orita-link に加えて **--orita-serif-local** も CSS 内供給として
除外すること（extend-head からは供給されない設計）。

**確認方法**：hugo server を再起動し、/ko/ の見出し（오리타 류마 등）が
明朝体系（명조체）に、/zh-cn/ の「折田 龙马」が全字そろった宋体に
なれば成功です。
